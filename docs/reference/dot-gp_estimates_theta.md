# Read the per-unit point-estimate vector (length J) from an eb_estimates

The installed ebrecipe eb_estimates stores point estimates as
`theta_hat`. The `estimate` fallback is retained only for older
stand-ins and cached tests.

## Usage

``` r
.gp_estimates_theta(estimates)
```
