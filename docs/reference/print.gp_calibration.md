# Print a `gp_calibration` – a compact, result-first one-liner

ASCII-only, returns invisibly. House-style result vocabulary only
(GP-DEC-14-A).

## Usage

``` r
# S3 method for class 'gp_calibration'
print(x, ...)
```

## Arguments

- x:

  A `gp_calibration`.

- ...:

  Ignored.
