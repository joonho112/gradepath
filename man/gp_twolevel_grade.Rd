% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/industry-grading.R
\name{gp_twolevel_grade}
\alias{gp_twolevel_grade}
\title{Grade two-level industry matrices}
\usage{
gp_twolevel_grade(
  twolevel = NULL,
  pairwise_theta = NULL,
  pairwise_bar = NULL,
  posterior = NULL,
  lambda = 0.25,
  lambda_grid = NULL,
  control = NULL,
  acceptance_mode = FALSE,
  build_report_cards = TRUE,
  ...
)
}
\arguments{
\item{twolevel}{A \code{gp_twolevel_quadrature}, \code{gp_twolevel_pi},
\code{gp_twolevel_pairwise_bundle}, or a plain list carrying \code{pairwise_theta} and
\code{pairwise_bar}. When \code{NULL} (default) the explicit \code{pairwise_theta} /
\code{pairwise_bar} arguments must be supplied instead. Passing an already-graded
\code{gp_twolevel_grade} returns it unchanged (no re-solve).}

\item{pairwise_theta, pairwise_bar}{Optional explicit \code{gp_pairwise} inputs for the
firm-level and between-industry routes. When \code{NULL} (default) they are taken
from \code{twolevel}; at least one source for each route must resolve.}

\item{posterior}{Optional two-level \code{gp_posterior} (e.g. from a two-level
posterior builder). When \code{NULL} (default) the routes are graded structurally
with no report cards. When supplied (and \code{build_report_cards = TRUE}) it backs
the assembled report cards.}

\item{lambda}{Numeric in \verb{[0, 1]}; the selected reporting penalty at which each
route's grade is read off the path. Default \code{0.25}.}

\item{lambda_grid}{Optional numeric vector; the penalty grid solved for each
route. When \code{NULL} (default) it is the package parity anchors plus \code{lambda}
(\code{0.25}, \code{1}, and \code{lambda}), sorted and de-duplicated.}

\item{control}{Optional \link{gp_control} threaded to the solves. When \code{NULL}
(default) the resolved \code{pairwise_theta}'s own control is used.}

\item{acceptance_mode}{Logical; the solver solution-quality / fallback policy
forwarded to the internal \code{\link[=gp_grade_path]{gp_grade_path()}} (Gurobi backend only). \code{FALSE}
(default) reports a gap/time-limit solve honestly with that status; \code{TRUE}
adds optimization attempts and never relabels a non-optimal solve as optimal.}

\item{build_report_cards}{Logical; when \code{TRUE} (default) and a \code{posterior} is
available, assemble the per-route \code{gp_report_card}s. When \code{FALSE} the routes
are graded structurally only.}

\item{...}{Reserved; an error is raised if any argument is passed.}
}
\value{
A validated \code{gp_twolevel_grade} object (a list of class
\code{c("gp_twolevel_grade", "list")}) with the public slots: \describe{
\item{\code{ids}, \code{industry_levels}}{Character vectors; the firm ids (\code{Pi_theta}
route) and industry ids (\code{Pi_bar} route).}
\item{\code{pairwise_theta}, \code{pairwise_bar}}{The \code{gp_pairwise} inputs actually
graded for the two routes.}
\item{\code{industry_rfe}}{Named list for the firm-level route: \code{grade_path},
\code{selected_grade} (at \code{lambda}), \code{grade_count}, \code{report_card} (or \code{NULL}),
\code{posterior}, and \code{producer_status}.}
\item{\code{btwn}}{The same payload for the between-industry route.}
\item{\code{posterior}}{The supplied two-level \code{gp_posterior}, or \code{NULL}.}
\item{\code{pi}, \code{pairwise}}{The two-level carriers (\code{gp_twolevel_pi},
\code{gp_twolevel_pairwise_bundle}) when supplied, else \code{NULL}.}
\item{\code{selected_lambda}, \code{lambda_grid}}{The selected penalty and the solved
grid.}
\item{\code{method}}{Character tag for how the inputs were resolved (e.g.
\code{"pairwise"}, \code{"pi"}).}
\item{\code{producer_status}}{Combined honest solver status across the two routes
(\code{"OK"} only when both routes are acceptance-ready).}
\item{\code{control}}{The validated \link{gp_control} used.}
\item{\code{provenance}, \code{schema_version}, \code{warnings}}{Internal audit slots.}
}
}
\description{
\code{gp_twolevel_grade()} routes the M2 two-level pairwise outputs into the package
grade solver without touching the one-level monolith: the \code{industry_rfe} route
grades the firm-level \code{Pi_theta}, and the \code{btwn} route grades the industry-level
\code{Pi_bar}. Feed it a bundle from \code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}} (or the lower-level
two-level carriers below). When a two-level posterior is supplied it also
assembles a firm \code{gp_report_card} for \code{industry_rfe} and an industry-level
\code{gp_report_card} for \code{btwn}, retrievable via \code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}}.
}
\details{
The returned object is an M2 grading surface, not the M2 acceptance scorecard and
not a paper industry DR / tau / R2 reproduction. Use \code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}} to
classify recorded or supplied fixture-gate evidence into \code{PROMOTED},
\code{APPROXIMATE_OK}, and the overall M2 status; see \code{\link[=gp_m2_status]{gp_m2_status()}}.
}
\examples{
# A small synthetic strict-preference pair (the package's own test pattern).
strict <- function(n, p) {
  id <- paste0(p, seq_len(n)); m <- matrix(0, n, n, dimnames = list(id, id))
  for (i in seq_len(n)) for (j in seq_len(n)) if (i != j) m[i, j] <- if (i < j) 0.95 else 0.05
  m
}
pw <- gp_twolevel_pairwise(strict(4, "f"), strict(3, "i"),
                           control = gp_control(backend = "highs"))

# The grade step solves a tiny integer program, so it needs a backend.
\donttest{
tlg <- gp_twolevel_grade(pw, control = gp_control(backend = "highs"),
                         lambda_grid = c(0.25, 1))
tlg
}

}
\seealso{
\code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}}, \code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}},
\code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}}, \code{\link[=gp_grade_path]{gp_grade_path()}}

Other gradepath-twolevel:
\code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}},
\code{\link[=gp_m2_promoted_registry]{gp_m2_promoted_registry()}},
\code{\link[=gp_m2_status]{gp_m2_status()}},
\code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}},
\code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}}
}
\concept{gradepath-twolevel}
