% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_pairwise}
\alias{get_pairwise}
\alias{get_pairwise.default}
\alias{get_pairwise.gp_fit}
\title{Extract the pairwise outranking object from a gradepath fit}
\usage{
get_pairwise(fit, ...)

\method{get_pairwise}{default}(fit, ...)

\method{get_pairwise}{gp_fit}(fit, ...)
}
\arguments{
\item{fit}{A \code{gp_fit} object.}

\item{...}{Unused; accepted for S3 method extensibility. No arguments are
forwarded; supplying any has no effect.}
}
\value{
The \code{gp_pairwise} object stored on \code{fit} (returned unchanged).
}
\description{
Returns the \code{gp_pairwise} slot of a \code{gp_fit} unchanged: the \eqn{J \times J}
posterior outranking matrix \eqn{\pi_{ij}} (the posterior probability that unit
\code{i} outranks unit \code{j}) together with its cleanup and source metadata. Reach for it
to inspect the social-choice input that the grade path is solved over.
}
\details{
Read-only accessor adopted in \code{GP-DEC-13-A}; it never recomputes and only reads
the materialized slot. The matrix encodes pairwise posterior order only and
carries no ranking-superiority statement of any kind.
}
\examples{
# Instant: read the pairwise object off the bundled tiny fit.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
get_pairwise(fit)      # <gp_pairwise>

}
\seealso{
\code{\link[=get_grades]{get_grades()}}, \code{\link[=get_report_card]{get_report_card()}}, \code{\link[=gp_pairwise]{gp_pairwise()}}

Other gradepath-accessors:
\code{\link[=as.data.frame.gp_fit]{as.data.frame.gp_fit()}},
\code{\link[=coef.gp_fit]{coef.gp_fit()}},
\code{\link[=get_control]{get_control()}},
\code{\link[=get_grades]{get_grades()}},
\code{\link[=get_posterior]{get_posterior()}},
\code{\link[=get_prior]{get_prior()}},
\code{\link[=get_report_card]{get_report_card()}}
}
\concept{gradepath-accessors}
