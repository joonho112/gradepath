% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2-acceptance.R
\name{gp_m2_promoted_registry}
\alias{gp_m2_promoted_registry}
\title{Effective M2 registry after fixture-gate promotion}
\usage{
gp_m2_promoted_registry(fixture_gates = NULL, registry = NULL)
}
\arguments{
\item{fixture_gates}{Optional fixture-gate evidence. When \code{NULL}
(default) the frozen race/gender fixture snapshot is used
instead of rerunning the fixture gate. May also be a live
\code{gp_twolevel_fixture_gate} object, a list of such objects, or an equivalent
data frame.}

\item{registry}{Optional registry data frame to promote. When \code{NULL} (default)
the package \link{gp_registry} is used.}
}
\value{
A registry data frame (a copy of the input) with the eligible L02
\code{class} entries promoted to \code{banded}, and two attributes attached:
\code{m2_fixture_gates} (the race/gender gate data frame used) and \code{m2_l02_ids}
(the L02 row ids considered for promotion).
}
\description{
\code{gp_m2_promoted_registry()} returns a copy of the package registry whose M2 L02
continuous-quantity \code{class} column is promoted from \code{approximate} to \code{banded}
only for panels whose fixture gate passes. The registry keeps these
rows \code{approximate} by default; this helper applies the panel-by-panel promotion
that \code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}} consumes.
}
\details{
With the default \code{fixture_gates = NULL} it uses the recorded
race/gender fixture snapshot: that snapshot leaves the race L02 rows
\code{approximate} (the race \code{Pi_theta} gap is \code{0.0121756787 > 0.01}) and promotes the
gender L02 rows to \code{banded} (the gender fixture gate passed). Supply live
fixture-gate evidence to recompute the promotion from fresh fixtures.

\code{PROMOTED} / \code{banded} means only that a panel's fixture artifacts met
the intermediate parity band used for M2 promotion. It does NOT mean the paper's
industry DR, tau, or R2 targets were directly reproduced or accepted.
}
\examples{
# Instant; no solve. The effective registry after fixture promotion.
reg <- gp_m2_promoted_registry()
attr(reg, "m2_l02_ids")

}
\seealso{
\code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}}, \code{\link[=gp_m2_status]{gp_m2_status()}}, \link{gp_registry}

Other gradepath-twolevel:
\code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}},
\code{\link[=gp_m2_status]{gp_m2_status()}},
\code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}},
\code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}},
\code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}}
}
\concept{gradepath-twolevel}
