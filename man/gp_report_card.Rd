% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-card.R
\name{gp_report_card}
\alias{gp_report_card}
\title{Assemble a unit-level report card}
\usage{
gp_report_card(
  estimates = NULL,
  posterior = NULL,
  selected_grade = NULL,
  grade_path = NULL,
  ...
)
}
\arguments{
\item{estimates}{One of: an \code{ebrecipe::eb_estimates}-like input container, a
\code{gp_fit} (its stored report card is returned as-is), a \code{gp_grade_path}, or a
\code{gp_grade_fit}. For stage-wise assembly it supplies the report-card \code{label},
\code{estimate}, and \code{se}; if omitted (\code{NULL}, the default) those columns are read
from \code{posterior}.}

\item{posterior}{A \code{gp_posterior} (from \code{\link[=get_posterior]{get_posterior()}}) giving each unit's
\code{posterior_mean}, \code{lower}, and \code{upper}. Required for stage-wise assembly;
ignored when \code{estimates} is a \code{gp_fit}.}

\item{selected_grade}{A \code{gp_grade_fit}, usually from \code{\link[=gp_select_grade]{gp_select_grade()}};
supplies the displayed \code{grade} column and the selected penalty. Required for
stage-wise assembly.}

\item{grade_path}{A solved \code{gp_grade_path}; its endpoint fit fixes the row
sort order (most-extreme grade first, ties broken by id). Required for
stage-wise assembly.}

\item{...}{Unused; reserved for future arguments. Passing any value raises an
error.}
}
\value{
A validated \code{gp_report_card} object (a list of class
\code{c("gp_report_card", "list")}) with the public slots: \describe{
\item{\code{ids}}{Character vector; the unit-id order of the table rows
(most-extreme grade first).}
\item{\code{table}}{Data frame, one row per unit, with \code{id}, \code{label}, the integer
\code{grade}, \code{sort_rank}, \code{selected_lambda}, the posterior summary
\code{posterior_mean}/\code{lower}/\code{upper}, and the original \code{estimate}/\code{se}.}
\item{\code{selected_lambda}}{Numeric; the penalty the displayed grades were taken
from.}
\item{\code{grades}}{Integer vector of the per-unit grade labels, aligned to \code{ids}.}
\item{\code{control}}{The \link{gp_control} carried from \code{grade_path}.}
\item{\code{provenance}, \code{warnings}, \code{schema_version}}{Producer metadata and
internal audit slots.}
}
}
\description{
\code{gp_report_card()} assembles the per-unit report-card table: each unit with
its grade label, posterior summary, and original estimate, sorted from
most-extreme to least-extreme. It does not solve a grade problem -- the row
order comes from the endpoint fit cached in \code{grade_path}, while the displayed
\code{grade} column comes from \code{selected_grade}. Pass a finished \code{gp_fit} to read
back its stored card, or the stage objects to assemble one yourself; the
result prints, plots with \code{\link[=gp_plot_report_card]{gp_plot_report_card()}}, and is what \code{\link[=get_report_card]{get_report_card()}}
returns.
}
\details{
Grade labels are integers in \verb{\{1, ..., n\}} and carry no
ranking-superiority statement of any kind: grade 1 is simply the most-extreme
theta block.
}
\note{
Welfare orientation (frozen INVARIANT 2). Grade 1 is the most-extreme
theta, and its welfare reading FLIPS with the application: for firms the
most-extreme block is the most discriminatory, while for names it is the
best-treated. The label is therefore not hard-coded to one application; the
console print and \code{\link[=format_gp_report_card_cli]{format_gp_report_card_cli()}} surface this same note. See
Appendix A.
}
\examples{
# Read the stored card from the bundled fit (no Gurobi, no solve).
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
gp_report_card(fit)              # returns fit$report_card

# Re-assemble the same card from the fit's stage slots (solve-free).
rc <- gp_report_card(
  estimates      = fit$estimates,
  posterior      = fit$posterior,
  selected_grade = fit$selected_grade,
  grade_path     = fit$grade_path
)
head(rc$table)                   # per-unit grade-label table

}
\seealso{
\code{\link[=gp_select_grade]{gp_select_grade()}}, \code{\link[=gp_grade_path]{gp_grade_path()}}, \code{\link[=get_report_card]{get_report_card()}},
\code{\link[=gp_plot_report_card]{gp_plot_report_card()}}, \code{\link[=format_gp_report_card_cli]{format_gp_report_card_cli()}}
}
\concept{gradepath-report-card}
