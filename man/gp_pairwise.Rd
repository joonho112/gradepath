% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwise.R
\name{gp_pairwise}
\alias{gp_pairwise}
\title{Pairwise outranking probability matrix}
\usage{
gp_pairwise(
  object = NULL,
  reporting_support = NULL,
  ids = NULL,
  control = NULL,
  assumption = "one_level_independence",
  ...,
  power = 0L
)
}
\arguments{
\item{object}{A \code{gp_fit} (read-only accessor path), the list returned by
\code{gp_posterior_weights()} carrying \verb{$W} (\verb{J x M}) and \verb{$reporting_support}
(\verb{M x J}), or a bare \verb{J x M} \code{W} matrix. A bare matrix is accepted only when
\code{reporting_support} is supplied or a shared axis is otherwise intended.
Default \code{NULL} requires the legacy \verb{weights =} alias (see \code{...}).}

\item{reporting_support}{Optional \verb{M x J} per-unit reporting axis; overrides
\code{object$reporting_support} on the weight-list path. \code{NULL} (default) takes the
axis from \code{object}. Ignored when \code{object} is a \code{gp_fit}.}

\item{ids}{Optional length-\code{J} identifiers. \code{NULL} (default) takes them from
\code{object$ids}, the row names of \code{W}, or \code{seq_len(J)}. Ignored when \code{object} is
a \code{gp_fit}.}

\item{control}{Optional \link{gp_control} recorded on the result. \code{NULL} (default)
uses \code{\link[=gp_control]{gp_control()}} on the weight-list path. Ignored when \code{object} is a
\code{gp_fit}.}

\item{assumption}{Character scalar; the pairwise independence assumption
recorded in \code{source}. Defaults to \code{"one_level_independence"} (the one-level
analytic path). Ignored when \code{object} is a \code{gp_fit}.}

\item{...}{Unused, except for the legacy named \verb{weights =} alias for \code{object};
any other argument raises an error.}

\item{power}{Integer scalar; the matrix-power option. Only the KRW / public
path \code{0L} (default) is implemented on the M1 surface; any other value errors.}
}
\value{
A validated \link{gp_pairwise} object (a list of class
\code{c("gp_pairwise", "list")}) with the public slots: \describe{
\item{\code{ids}}{Character vector of length \code{J}; the canonical unit-id order, and
the row/column names of \code{matrix}.}
\item{\code{matrix}}{Numeric \verb{J x J} matrix of outranking probabilities in
\verb{[0, 1]}; \code{matrix[i, j] = Pr(theta_i > theta_j)}, diagonal \code{0.5}.}
\item{\code{power}}{Integer; the matrix-power option (\code{0L}).}
\item{\code{cleanup}}{Named list (\code{antisymmetry}, \code{diagonal}, \code{zero_floor}); the
applied cleanup contract -- antisymmetry on, diagonal \code{0.5}, one-sided
\code{1e-7} floor.}
\item{\code{source}}{Named list (\code{stage}, \code{rule}, \code{assumption}); how the matrix
was produced and under which independence assumption.}
\item{\code{control}}{The \link{gp_control} recorded on the result.}
\item{\code{warnings}, \code{schema_version}, \code{provenance}}{Internal audit slots.}
}
}
\description{
\code{gp_pairwise()} returns the \verb{J x J} pairwise outranking matrix whose entry
\code{pi[i, j]} is the posterior probability \code{Pr(theta_i > theta_j)}. Given a
materialized \code{gp_fit} it is a read-only accessor for the stored matrix; given
the output of \code{gp_posterior_weights()} (or a bare \verb{J x M} weight matrix plus a
reporting axis) it computes the matrix by exact posterior-CDF integration and
applies the frozen cleanup contract. The result is the outranking structure the
grade engine (\code{\link[=gp_grade_path]{gp_grade_path()}}, \code{\link[=gp_grade]{gp_grade()}}) scores.
}
\details{
On the compute path the entry is the per-unit CDF integral
\verb{pi[i, j] = sum_m W[i, m] * F_j(theta_i[m]^-)}, where \code{F_j} is unit \code{j}'s
posterior CDF strictly below the argument (ties contribute \code{0}). Cleanup is
applied in the frozen order antisymmetry (\code{pi[j, i] = 1 - pi[i, j]}) ->
diagonal set to \code{0.5} -> zero-floor: every off-diagonal value below \code{1e-7} is
raised to \code{1e-7} (a one-sided floor; exact \code{1} stays exact \code{1}), so final
antisymmetry is exact up to the floor tolerance. See KRW (2024, Section 4) and
the pairwise method vignette.
}
\examples{
# Read-only accessor: pull the stored pairwise matrix from the bundled fit.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
pw <- gp_pairwise(fit)
pw                              # a 24 x 24 gp_pairwise
pw$matrix[1:4, 1:4]             # the outranking probabilities, diagonal 0.5

}
\seealso{
\code{\link[=gp_grade_path]{gp_grade_path()}}, \code{\link[=gp_grade]{gp_grade()}}, \code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_report_card]{gp_report_card()}}
}
\concept{gradepath-pairwise}
