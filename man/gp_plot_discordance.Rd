% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-report-card.R
\name{gp_plot_discordance}
\alias{gp_plot_discordance}
\title{Plot the KRW conditional-discordance heatmap (lower triangle)}
\usage{
gp_plot_discordance(x, annotate = TRUE, ...)
}
\arguments{
\item{x}{A \code{gp_frontier} (reads \code{x$dr_matrix}), a \code{gp_fit} (its frontier is
rebuilt with \code{\link[=gp_frontier]{gp_frontier()}} from \code{x$grade_path} and \code{x$pairwise}), or a
numeric conditional-DR matrix (square; lower triangle populated, upper
triangle and diagonal \code{NA}).}

\item{annotate}{Logical; write each cell's DR value (\code{"\%.3f"}) with an
auto-contrasting colour. Default \code{TRUE}.}

\item{...}{Reserved for future arguments; currently unused.}
}
\value{
A \code{ggplot} object (class \code{c("gg", "ggplot")}); it renders only when
printed.
}
\description{
\code{gp_plot_discordance()} draws the Kline-Rose-Walters conditional discordance
matrix (their Figure 10a / 17a) as a lower-triangular heatmap. The cell for a
grade pair is the conditional discordance rate \eqn{DR_{g,g'}} between the
more-extreme grade \code{g} (columns, grade 1 on the left) and the less-extreme
grade \verb{g'} (rows, grade 2 at the top): the posterior share of disagreeing
rankings among cross-grade pairs. Higher DR is darker; with \code{annotate = TRUE}
the value is written in each cell with auto-contrasting text. Pass it a
\code{\link[=gp_frontier]{gp_frontier()}} (or, for convenience, the \code{gp_fit} it was built from, or a
bare DR matrix).
}
\examples{
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))

# Build a frontier from the fit's grade path and pairwise.
fr <- gp_frontier(fit$grade_path, pairwise = get_pairwise(fit))

gp_plot_discordance(fr)                   # annotated lower-triangular heatmap
gp_plot_discordance(fr, annotate = FALSE) # heatmap without cell values
gp_plot_discordance(fit)                  # convenience: a gp_fit
gp_plot_discordance(fr$dr_matrix)         # a bare dr_matrix

}
\seealso{
\code{\link[=gp_plot_report_card]{gp_plot_report_card()}}, \code{\link[=theme_gradepath]{theme_gradepath()}}, \code{\link[=gp_frontier]{gp_frontier()}}

Other gradepath-plots:
\code{\link[=autoplot.gp_frontier]{autoplot.gp_frontier()}},
\code{\link[=autoplot.gp_report_card]{autoplot.gp_report_card()}},
\code{\link[=gp_plot_frontier]{gp_plot_frontier()}},
\code{\link[=gp_plot_posterior_contrast]{gp_plot_posterior_contrast()}},
\code{\link[=gp_plot_report_card]{gp_plot_report_card()}},
\code{\link[=scale_color_gradepath]{scale_color_gradepath()}},
\code{\link[=theme_gradepath]{theme_gradepath()}}
}
\concept{gradepath-plots}
