% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness-check.R
\name{gp_check}
\alias{gp_check}
\title{Compare a replicated value against the gradepath registry}
\usage{
gp_check(
  id,
  replicated = NA_real_,
  producer_status = "OK",
  registry = NULL,
  label = NULL
)
}
\arguments{
\item{id}{Character scalar; a registry key present in \code{gp_registry$id}. An id not
in the registry raises an error.}

\item{replicated}{Scalar replicated value, already converted to the registry unit
(e.g. pass a percent for rows whose \code{unit} is \code{"percent"}). Defaults to
\code{NA_real_}, which yields an \code{n/a} status (nothing to compare).}

\item{producer_status}{Character scalar; the runtime status of the producing
stage, normalized internally. Defaults to \code{"OK"}. Any non-\code{OK} status short-
circuits the numeric comparison and returns status \code{UNVERIFIED} (the producing
stage was not acceptance-ready), regardless of \code{replicated}.}

\item{registry}{Registry data frame to look \code{id} up in. Defaults to \code{NULL}, which
uses the bundled package data \link{gp_registry}.}

\item{label}{Character scalar quantity label overriding \code{registry$quantity} in the
returned row. Defaults to \code{NULL} (use the registry's own \code{quantity} text).}
}
\value{
A one-row \code{data.frame} describing the comparison. \describe{
\item{\code{id}}{The registry id checked.}
\item{\code{quantity}}{Human-readable target description (\code{label} if supplied, else
the registry \code{quantity}).}
\item{\code{paper}}{Numeric published value from the registry.}
\item{\code{replicated}}{Numeric replicated value compared (\code{NA} when the producer
status is non-\code{OK}).}
\item{\code{delta}}{\code{replicated - paper} (\code{NA} when not compared).}
\item{\code{tol}}{Absolute PASS tolerance from the registry.}
\item{\code{unit}, \code{class}, \code{milestone}}{The registry row's unit, tolerance class,
and milestone, passed through.}
\item{\code{status}}{One of \code{PASS}, \code{FAIL}, \code{n/a} (missing value), \code{no-tol} (missing
tolerance), or \code{UNVERIFIED} (non-\code{OK} producer status).}
\item{\code{reason}}{Short reason string when not \code{PASS} (e.g. \code{outside_tolerance},
\code{missing_value}, \code{missing_tolerance}); \code{NA} on \code{PASS}.}
\item{\code{producer_status}}{The normalized producer status used.}
}
}
\description{
\code{gp_check()} is the single registry comparator used by the replication harness.
It looks up one row of \link{gp_registry} by \code{id}, reads that row's published paper
value and absolute tolerance, compares your already-on-unit \code{replicated} value to
it, and returns a one-row verdict data frame. The harness embeds no tolerances of
its own -- every band comes from the registry -- and \code{\link[=gp_run_all]{gp_run_all()}} is the vector
driver that calls this once per target.
}
\details{
The comparison is the single Chapter 16/17 absolute-band rule: \code{PASS} iff
\code{abs(replicated - paper) <= tol} (with a tiny \code{1e-9} numerical slack), \code{FAIL}
otherwise. This is a verification check against published values; it makes no
ranking-superiority statement of any kind.
}
\examples{
# Instant: compare a replicated count against a known registry target. The
# firm-count target (id "scale_n_firms_graded") is published as 97, tolerance 0.
gp_check("scale_n_firms_graded", replicated = 97)   # status PASS
gp_check("scale_n_firms_graded", replicated = 96)   # status FAIL (outside tol 0)

}
\seealso{
\code{\link[=gp_run_all]{gp_run_all()}}, \code{\link[=gp_validate_targets]{gp_validate_targets()}}, \link{gp_registry}

Other gradepath-harness:
\code{\link[=gp_run_all]{gp_run_all()}},
\code{\link[=gp_validate_targets]{gp_validate_targets()}}
}
\concept{gradepath-harness}
