% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frontier.R
\name{gp_frontier}
\alias{gp_frontier}
\title{Summarize the frontier of a solved grade path}
\usage{
gp_frontier(
  grade_path,
  pairwise = NULL,
  selected_lambda = NULL,
  benchmark_scores = NULL
)
}
\arguments{
\item{grade_path}{A solved \code{gp_grade_path} (from \code{\link[=gp_grade_path]{gp_grade_path()}} or the
\code{grade_path} slot of a \code{gp_fit}). Supplies the penalty grid and the per-penalty
grade assignments the frontier is built over.}

\item{pairwise}{Optional \code{gp_pairwise} (from \code{\link[=gp_pairwise]{gp_pairwise()}} or \code{\link[=get_pairwise]{get_pairwise()}}).
When \code{NULL} (default) \code{gp_frontier()} returns only the frontier table cached by
\code{\link[=gp_grade_path]{gp_grade_path()}}; the benchmarks and conditional discordance matrix are left
empty. Supplying it must match \code{grade_path$ids} and unlocks both. Its \code{ids} must
equal \code{grade_path$ids}.}

\item{selected_lambda}{Optional numeric penalty at which the conditional
discordance matrix is evaluated. \code{NULL} (default) uses
\code{grade_path$selection$selected_lambda}; any value supplied must match a penalty
on the grid exactly.}

\item{benchmark_scores}{Optional named list (or one-row-per-unit data frame) of
the per-unit scores the naive baselines are built from. Requires three numeric
vectors named \code{raw_estimate}, \code{posterior_mean}, and \code{linear_shrinkage}, each one
value per unit and named by (or column-aligned to) the unit ids in
\code{pairwise$ids}; they are reordered to that id order. From these, five baseline
grade scorings are formed -- a rank cut on each of the three vectors plus decile
and quartile bins of \code{posterior_mean} -- and scored against the pairwise profile.
\code{NULL} (default) leaves the benchmark table empty. Requires \code{pairwise}; passing
scores without it raises an error.}
}
\value{
A validated \code{gp_frontier} object (a list of class
\code{c("gp_frontier", "list")}) with the public slots: \describe{
\item{\code{ids}}{Character vector; the canonical unit-id order, equal to
\code{grade_path$ids}.}
\item{\code{table}}{Data frame, one row per penalty on the grid, with \code{lambda},
\code{grade_count}, \code{discordance_rate}, \code{reliability}, and \code{tau_bar} (the average
between-grade rank distance), plus the solve \code{objective}/\code{status} when a
\code{pairwise} profile is supplied.}
\item{\code{benchmarks}}{Data frame of the five naive baseline scorings
(\code{raw_estimate_rank}, \code{posterior_mean_rank}, \code{linear_shrinkage_rank},
\code{posterior_mean_decile}, \code{posterior_mean_quartile}), each with its
\code{grade_count}, \code{discordance_rate}, \code{reliability}, and \code{tau_bar}; empty when
\code{benchmark_scores} (and \code{pairwise}) were not supplied.}
\item{\code{dr_matrix}}{Lower-triangular matrix of conditional discordance rates
between grade pairs at the selected penalty (row = less-extreme grade,
column = more-extreme grade; diagonal and upper triangle are \code{NA}); a
0 x 0 matrix when no \code{pairwise} profile was supplied.}
\item{\code{selection}}{Named list recording \code{selected_lambda}, the penalty the
conditional discordance matrix was evaluated at.}
\item{\code{control}}{The \link{gp_control} carried from \code{grade_path}.}
\item{\code{provenance}, \code{warnings}, \code{schema_version}}{Producer metadata and
internal audit slots; \code{warnings} notes when benchmarks and the conditional
discordance matrix were skipped for lack of a \code{pairwise} profile.}
}
}
\description{
\code{gp_frontier()} collects the frontier metrics for a solved grade path: the
per-penalty table of grade count, discordance rate, reliability, and average
rank distance, and -- when you also supply the pairwise outranking profile --
the five naive benchmark scorings and the selected-penalty conditional
discordance matrix. Reach for it after \code{\link[=gp_grade_path]{gp_grade_path()}} when you want the
frontier as a tidy object to print, plot with \code{\link[=gp_plot_frontier]{gp_plot_frontier()}}, or compare
grades against simple baselines.
}
\details{
The frontier metrics are computed over between-grade blocks of the
pairwise outranking profile and normalized by \code{2 * choose(n, 2)}. The
\code{dr_matrix} reports, for each pair of grades, the share of cross-grade
comparisons that run against the grade ordering (discordant), conditional on
that pair. Grade labels are integers and carry no ranking-superiority statement;
the benchmarks are descriptive baselines, not a contest.
}
\examples{
# Inspect the frontier of the bundled pre-solved fit (no Gurobi, no solve).
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
post <- get_posterior(fit)
ids <- get_pairwise(fit)$ids

fr <- gp_frontier(
  fit$grade_path,
  pairwise = get_pairwise(fit),
  benchmark_scores = list(
    raw_estimate     = setNames(post$estimate, ids),
    posterior_mean   = setNames(post$posterior_mean, ids),
    linear_shrinkage = setNames(0.5 * post$estimate, ids)
  )
)
fr$table        # per-penalty frontier metrics
fr$benchmarks   # the five naive baseline scorings

}
\seealso{
\code{\link[=gp_grade_path]{gp_grade_path()}}, \code{\link[=gp_pairwise]{gp_pairwise()}}, \code{\link[=gp_plot_frontier]{gp_plot_frontier()}},
\code{\link[=gp_plot_discordance]{gp_plot_discordance()}}, \code{\link[=get_pairwise]{get_pairwise()}}

Other gradepath-frontier:
\code{\link[=gp_krw_r2]{gp_krw_r2()}},
\code{\link[=gp_r2]{gp_r2()}}
}
\concept{gradepath-frontier}
