% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness-scorecard.R
\name{gp_run_all}
\alias{gp_run_all}
\title{Run a registry-driven gradepath scorecard}
\usage{
gp_run_all(
  replicated = NULL,
  targets = NULL,
  registry = NULL,
  producer_status = NULL
)
}
\arguments{
\item{replicated}{What to score. A \code{gp_fit} (its registry targets -- firm count,
grade count, discordance rate, tau, and for race the worst/best-grade counts --
are derived and scored); a data frame with \code{id} and \code{replicated} columns
(optionally \code{producer_status}, \code{group}); a named atomic vector or named list of
\code{id = value}; or \code{NULL} (default) for an \code{UNVERIFIED} skeleton over \code{targets}.}

\item{targets}{Optional character vector of registry ids to score (and the row
order). Defaults to \code{NULL}, which means all \code{M1} registry rows when \code{replicated}
is \code{NULL}, otherwise the ids present in \code{replicated}. Requested ids absent from
\code{replicated} are added as \code{UNVERIFIED} rows.}

\item{registry}{Registry data frame. Defaults to \code{NULL}, which uses the bundled
package data \link{gp_registry}.}

\item{producer_status}{Optional character producer-status override. Defaults to
\code{NULL}: a \code{gp_fit} input then uses \code{fit$provenance$producer_status} when present
and the fail-safe \code{"UNVERIFIED"} when absent; other replicated inputs (named
vectors / lists / data frames) default to \code{"OK"}, the caller-asserts-OK contract
for explicit replicated values.}
}
\value{
A list of class \code{c("gp_scorecard", "list")}. \describe{
\item{\code{table}}{Per-target verdict data frame (the bound \code{\link[=gp_check]{gp_check()}} rows with a
leading \code{group} column); \code{checks} is a synonym slot with the same content.}
\item{\code{summary}}{Per-\code{group} and \code{TOTAL} tally of \code{pass} / \code{fail} /
\code{unverified} / \code{n_a} / \code{no_tol} counts and a \code{pass_rate}.}
\item{\code{pass_rate}}{Numeric overall PASS fraction among scored (\code{PASS} + \code{FAIL})
rows, or \code{NA} when none are scored.}
\item{\code{provenance}, \code{warnings}}{Internal audit slots.}
}
}
\description{
\code{gp_run_all()} is the vector driver over \code{\link[=gp_check]{gp_check()}}: it applies the registry
comparator to a set of replicated values (or to a \code{gp_fit}, whose own targets it
extracts) and returns a \code{gp_scorecard} with the per-target table plus a grouped
pass/fail summary. Passing \code{replicated = NULL} returns the requested targets as
\code{UNVERIFIED} (a coverage skeleton with nothing solved). \code{\link[=gp_validate_targets]{gp_validate_targets()}} is
the thin verification-step alias.
}
\details{
This is a verification scorecard against published KRW (2024) values; it makes no
ranking-superiority statement of any kind. Bundled artifacts record the
current M1 \code{NOT_ACCEPTED} gate: unresolved or non-\code{OK} producer rows stay
\code{UNVERIFIED} rather than counting as \code{PASS}.
}
\examples{
# Instant, no solve: score explicit replicated values against the registry.
# Named vector of id = value; the firm-count target is published as 97.
sc <- gp_run_all(c(scale_n_firms_graded = 97))
sc$table[, c("id", "paper", "replicated", "status")]
sc$pass_rate

# NULL replicated returns the requested target as an UNVERIFIED skeleton.
gp_run_all(NULL, targets = "scale_n_firms_graded")$table$status

\donttest{
# A gp_fit input derives and scores the fit's own registry targets.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
gp_run_all(fit)$summary
}

}
\seealso{
\code{\link[=gp_validate_targets]{gp_validate_targets()}}, \code{\link[=gp_check]{gp_check()}}, \link{gp_registry}

Other gradepath-harness:
\code{\link[=gp_check]{gp_check()}},
\code{\link[=gp_validate_targets]{gp_validate_targets()}}
}
\concept{gradepath-harness}
