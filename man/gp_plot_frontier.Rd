% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-frontier.R
\name{gp_plot_frontier}
\alias{gp_plot_frontier}
\title{Plot the KRW information-reliability frontier}
\usage{
gp_plot_frontier(
  x,
  highlight = "selected",
  benchmarks = TRUE,
  annotate = TRUE,
  pairwise = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{gp_frontier} (primary). For convenience a \code{gp_fit} or
\code{gp_grade_path} is also accepted and converted with \code{\link[=gp_frontier]{gp_frontier()}};
benchmarks are shown only if already present (they are never force-derived
from a bare fit or path).}

\item{highlight}{Which slice to emphasise: \code{"selected"} (default) marks the
selected lambda; \code{"all"} marks every path point; a numeric value marks the
nearest path lambda.}

\item{benchmarks}{Logical; overlay the naive benchmark rules as labelled
shapes when they are present on the frontier. Default \code{TRUE}.}

\item{annotate}{Logical; report the highlighted \verb{(1 - DR, tau_bar)} point in
the subtitle. Default \code{TRUE}.}

\item{pairwise}{Optional \code{gp_pairwise}, used only when \code{x} is a \code{gp_grade_path}
(or to override a fit's stored pairwise) so the frontier can be rebuilt.}

\item{...}{Reserved for future arguments; currently unused.}
}
\value{
A \code{ggplot} object (class \code{c("gg", "ggplot")}); it renders only when
printed and stays fully overridable (add \verb{+ theme_*()} / \verb{+ scale_*}).
}
\description{
\code{gp_plot_frontier()} draws the Kline-Rose-Walters information-reliability
frontier (their Figure 6b / 13b): the optimal-grade path traced in reliability
\code{1 - DR} (x-axis) against the posterior expected Kendall agreement
\eqn{\bar{\tau}} (y-axis). Each frontier-table row is one point and the points
are connected as the Pareto path. The selected-lambda slice is marked with a
highlighted point, and the five naive benchmark rules are overlaid as labelled
shapes when the frontier carries them. Feed it a \code{\link[=gp_frontier]{gp_frontier()}} (or, for
convenience, the \code{gp_fit} or \code{gp_grade_path} it was built from).
}
\examples{
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))

# Build a frontier with the three shrinkage benchmark scores on the fit's ids.
post <- get_posterior(fit)
ids <- get_pairwise(fit)$ids
fr <- gp_frontier(
  fit$grade_path,
  pairwise = get_pairwise(fit),
  benchmark_scores = list(
    raw_estimate     = setNames(post$estimate, ids),
    posterior_mean   = setNames(post$posterior_mean, ids),
    linear_shrinkage = setNames(0.5 * post$estimate, ids)
  )
)

gp_plot_frontier(fr)                    # selected slice + benchmarks
gp_plot_frontier(fr, highlight = "all") # mark every path point
gp_plot_frontier(fit)                   # convenience: a gp_fit

}
\seealso{
\code{\link[=gp_plot_posterior_contrast]{gp_plot_posterior_contrast()}}, \code{\link[=gp_plot_discordance]{gp_plot_discordance()}},
\code{\link[=theme_gradepath]{theme_gradepath()}}, \code{\link[=gp_frontier]{gp_frontier()}}

Other gradepath-plots:
\code{\link[=autoplot.gp_frontier]{autoplot.gp_frontier()}},
\code{\link[=autoplot.gp_report_card]{autoplot.gp_report_card()}},
\code{\link[=gp_plot_discordance]{gp_plot_discordance()}},
\code{\link[=gp_plot_posterior_contrast]{gp_plot_posterior_contrast()}},
\code{\link[=gp_plot_report_card]{gp_plot_report_card()}},
\code{\link[=scale_color_gradepath]{scale_color_gradepath()}},
\code{\link[=theme_gradepath]{theme_gradepath()}}
}
\concept{gradepath-plots}
