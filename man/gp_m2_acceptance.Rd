% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2-acceptance.R
\name{gp_m2_acceptance}
\alias{gp_m2_acceptance}
\title{Build the M2 acceptance scorecard}
\usage{
gp_m2_acceptance(fixture_gates = NULL, registry = NULL)
}
\arguments{
\item{fixture_gates}{Optional fixture-gate evidence. When \code{NULL}
(default) the frozen race/gender fixture snapshot is used
instead of rerunning the slow fixture gate. May also be a live
\code{gp_twolevel_fixture_gate} object, a list of such objects, or an equivalent
data frame, to recompute the promotion decision from fresh evidence.}

\item{registry}{Optional registry data frame supplying the L01 / L02 / N10
target rows. When \code{NULL} (default) the package \link{gp_registry} is used.}
}
\value{
A validated \code{gp_m2_acceptance} object (a list of class
\code{c("gp_m2_acceptance", "list")}) with the public slots: \describe{
\item{\code{table}}{Data frame; the full M2 ledger, one row per gate
(\code{M2_ACCEPTANCE}, \code{L01}, \code{L02}, \code{N10}, \code{OVERRIDE}) with \code{status}, \code{reason},
\code{producer_status}, registry/effective class, paper/replicated values, and
notes. The \code{M2_ACCEPTANCE} row carries the overall formal status
(\code{PARTIAL_ACCEPTED} by default).}
\item{\code{checks}}{An alias of \code{table}.}
\item{\code{effective_registry}}{Registry data frame after panel-by-panel L02
promotion (see \code{\link[=gp_m2_promoted_registry]{gp_m2_promoted_registry()}}).}
\item{\code{fixture_gates}}{The race/gender fixture-gate data frame used.}
\item{\code{summary}}{One-row data frame counting rows by status (\code{pass},
\code{promoted}, \code{approximate_ok}, \code{evidence_ok}, \code{fail}).}
\item{\code{provenance}, \code{warnings}}{Internal audit slots.}
}
}
\description{
\code{gp_m2_acceptance()} assembles the M2 L01 / L02 / N10 ledger and the overall
formal M2 status as a \code{gp_m2_acceptance} object. With the default
\code{fixture_gates = NULL} it uses the recorded fixture snapshot
rather than rerunning the slow fixture gate; pass live fixture-gate evidence to
rebuild the scorecard. For a one-screen summary use \code{\link[=gp_m2_status]{gp_m2_status()}}.
}
\details{
The default recorded scorecard is \code{PARTIAL_ACCEPTED}: L01 industry grade-count
rows pass exactly; N10 rows are synthetic support-guard evidence rather than
registered paper-value passes; gender L02 continuous rows are \code{PROMOTED} to
\code{banded} by fixture parity; and race L02 continuous rows remain \code{APPROXIMATE_OK}
because the recorded race \code{Pi_theta} fixture gap is \code{0.0121756787 > 0.01}.

\code{PROMOTED} / \code{banded} means only that a panel's fixture artifacts met
the intermediate parity band used for M2 promotion. It does NOT mean the paper's
industry DR, tau, or R2 targets were directly reproduced or accepted; some
continuous L02 rows are fixture-parity evidence, not direct reproduction.
}
\examples{
# Instant; no solve. The default recorded M2 scorecard.
acc <- gp_m2_acceptance()
acc$summary

}
\seealso{
\code{\link[=gp_m2_status]{gp_m2_status()}}, \code{\link[=gp_m2_promoted_registry]{gp_m2_promoted_registry()}}, \code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}},
\code{\link[=gp_check]{gp_check()}}

Other gradepath-twolevel:
\code{\link[=gp_m2_promoted_registry]{gp_m2_promoted_registry()}},
\code{\link[=gp_m2_status]{gp_m2_status()}},
\code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}},
\code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}},
\code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}}
}
\concept{gradepath-twolevel}
