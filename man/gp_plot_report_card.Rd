% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-report-card.R
\name{gp_plot_report_card}
\alias{gp_plot_report_card}
\title{Plot the KRW report card (horizontal point-and-interval)}
\usage{
gp_plot_report_card(
  x,
  order = "rank",
  ci_level = NULL,
  max_rows = NULL,
  zero_line = "auto",
  ...
)
}
\arguments{
\item{x}{A \code{gp_report_card} (primary). A \code{gp_fit} is also accepted, in which
case its \verb{$report_card} is used.}

\item{order}{Row order, most-extreme unit on top. \code{"rank"} (default) uses the
endpoint/Condorcet \code{sort_rank} (the canonical endpoint-sorted figure);
\code{"mean"} orders by \code{posterior_mean}; \code{"grade"} groups by grade then
\code{sort_rank}; \code{"label"} is alphabetical.}

\item{ci_level}{Optional number in \verb{(0, 1)} used ONLY to label the caption,
e.g. \code{0.9} -> "90\% credible interval". \code{NULL} (default) reads the level from
\code{x$control$interval_level} (default \code{0.90}); the interval itself is never
recomputed.}

\item{max_rows}{Optional positive integer keeping only the top \code{max_rows}
units by the chosen order, with a "Showing N of M" subtitle (never silent).
\code{NULL} (default) shows all units.}

\item{zero_line}{Dashed reference line at \code{x = 0}: \code{"auto"} (default) draws it
only when the intervals straddle zero (\verb{min(lower) < 0 < max(upper)});
\code{"on"} always; \code{"off"} never.}

\item{...}{Reserved for future arguments; currently unused.}
}
\value{
A \code{ggplot} object (class \code{c("gg", "ggplot")}); it renders only when
printed.
}
\description{
\code{gp_plot_report_card()} draws the Kline-Rose-Walters unit-level report card
(their Figures 7, 8, 14, 15) as a horizontal point-and-interval
("caterpillar") plot: each unit's posterior mean (point) with its stored
credible interval (\code{lower}, \code{upper}), one unit per row, ordered so the
most-extreme unit is on top, and coloured by the unit's selected grade using
the shared ordinal palette (\code{\link[=scale_color_gradepath]{scale_color_gradepath()}}). The interval already
on the card is displayed as-is and never recomputed. The figure is neutral: it
carries no welfare-verdict text, because grade 1's welfare meaning flips
between firms and names. Pass it a \code{\link[=gp_report_card]{gp_report_card()}} (or the \code{gp_fit} that
carries one).
}
\examples{
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
card <- get_report_card(fit)

gp_plot_report_card(card)                  # endpoint-sorted, coloured by grade
gp_plot_report_card(card, order = "grade") # grouped by grade
gp_plot_report_card(card, max_rows = 10)   # top 10 only
gp_plot_report_card(fit)                   # convenience: a gp_fit

}
\seealso{
\code{\link[=gp_plot_discordance]{gp_plot_discordance()}}, \code{\link[=theme_gradepath]{theme_gradepath()}},
\code{\link[=scale_color_gradepath]{scale_color_gradepath()}}, \code{\link[=gp_report_card]{gp_report_card()}}

Other gradepath-plots:
\code{\link[=autoplot.gp_frontier]{autoplot.gp_frontier()}},
\code{\link[=autoplot.gp_report_card]{autoplot.gp_report_card()}},
\code{\link[=gp_plot_discordance]{gp_plot_discordance()}},
\code{\link[=gp_plot_frontier]{gp_plot_frontier()}},
\code{\link[=gp_plot_posterior_contrast]{gp_plot_posterior_contrast()}},
\code{\link[=scale_color_gradepath]{scale_color_gradepath()}},
\code{\link[=theme_gradepath]{theme_gradepath()}}
}
\concept{gradepath-plots}
