% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{gp_preview}
\alias{gp_preview}
\title{Preview a gradepath run without solving}
\usage{
gp_preview(
  data,
  demographic = c("race", "gender"),
  groups = NULL,
  control = gp_control(),
  ...
)
}
\arguments{
\item{data}{Data frame, list, or \code{ebrecipe::eb_estimates} object carrying
\code{theta_hat}/\code{s} (or the demographic-specific \code{theta_hat_race}/\code{se_race},
\code{theta_hat_gender}/\code{se_gender}) plus an optional \code{unit_id}/\code{firm_id}/\code{label}.
Do not supply both a generic and a demographic-specific column for the same
quantity in one input.}

\item{demographic}{Character scalar, \code{"race"} or \code{"gender"}; selects the
estimate columns the input grammar reads. Defaults to \code{"race"}.}

\item{groups}{Optional grouping vector, or \code{NULL}. Grouped/two-level execution
is not implemented in the M1 monolith; a non-\code{NULL} value is previewed as a
pending grouped workflow (with an explanatory warning and a \code{GROUPS_ERROR}
status), not run. Defaults to \code{NULL} (the one-level workflow).}

\item{control}{A \link{gp_control} object, or \code{NULL} to use the defaults. Supplies
the backend and the lambda grid the preview reports. Defaults to
\code{\link[=gp_control]{gp_control()}}.}

\item{...}{Unused; an error is raised if any argument is passed.}
}
\value{
A validated S3 object of class \code{c("gp_preview", "list")}: the planned
run with no results. \describe{
\item{\code{ids}}{Character vector; the canonical unit-id order parsed from the
input.}
\item{\code{n_units}}{Integer scalar; the number of units (equals
\code{length(ids)}).}
\item{\code{demographic}}{Character scalar; \code{"race"} or \code{"gender"}.}
\item{\code{groups}}{Character vector of length \code{n_units}, or \code{NULL}; the parsed
grouping when one was supplied.}
\item{\code{workflow}}{Character scalar; the inferred workflow, either
\code{"one_level_independence"} or \code{"grouped_pending"}.}
\item{\code{lambda_grid}}{Numeric vector; the operational frontier penalty grid
the run would sweep.}
\item{\code{estimated_solves}}{Integer scalar; the number of grade-IP solves the
run would perform (the length of \code{lambda_grid}).}
\item{\code{backend}}{Character scalar; the grade-IP backend the run would use.}
\item{\code{control}}{The validated \link{gp_control} the preview was built from.}
\item{\code{status}}{A \code{gp_status}; \code{OK} for a one-level plan, or \code{GROUPS_ERROR}
when a grouping was supplied.}
\item{\code{schema_version}, \code{provenance}, \code{warnings}}{Internal audit slots; the
\code{provenance} stamp records that no solve was performed.}
}
}
\description{
\code{gp_preview()} is the solve-free dry run for the one-level KRW workflow. Give
it the same firm-level input and demographic you would hand to
\code{\link[=krw_report_card]{krw_report_card()}}; it validates the input grammar and reports the inferred
plan -- the unit ids, the inferred workflow, the chosen backend, the
operational frontier penalty grid, and the number of grade-IP solves the run
would perform -- then returns a \code{gp_preview} object WITHOUT running the
beta-GMM, deconvolution, posterior, pairwise, or any solver stage. Reach for
it to size and sanity-check a run before committing to a full solve.
}
\examples{
# Solve-free: load the bundled tiny example input and preview the planned run
# (no Gurobi, no solve, instant).
inp <- readRDS(system.file("extdata/examples/tiny_input.rds", package = "gradepath"))
pv <- gp_preview(inp, demographic = "race")
pv                  # result-first console print of the planned run
pv$n_units          # number of units the run would grade
pv$estimated_solves # number of grade-IP solves the run would perform

}
\seealso{
\code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_grade_path]{gp_grade_path()}}, \code{\link[=gp_control]{gp_control()}}

Other gradepath-grade:
\code{\link[=gp_grade]{gp_grade()}},
\code{\link[=gp_grade_path]{gp_grade_path()}},
\code{\link[=gp_select_grade]{gp_select_grade()}}
}
\concept{gradepath-grade}
