% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/krw-r2.R
\name{gp_krw_r2}
\alias{gp_krw_r2}
\title{KRW grade R-squared from the rsquared.do recipe}
\usage{
gp_krw_r2(theta, ranking, grade_col = NULL, scale = c("percent", "proportion"))
}
\arguments{
\item{theta}{Data frame shaped like \code{theta_estimates_race.csv} /
\code{theta_estimates_gender.csv}: it must carry \code{log_dif} and (strictly positive)
\code{log_dif_se}, and optionally \code{obs_idx}. When \code{obs_idx} is absent, row order
defines it, matching \code{rsquared.do}.}

\item{ranking}{Data frame shaped like \verb{ranking_results_*_*.csv}: it must carry
\code{obs_idx}, \code{post_mean_beta}, \code{pmean_sq}, and a grade column. Joined to \code{theta}
on \code{obs_idx}; every \code{theta} id must be present here.}

\item{grade_col}{Character scalar naming the grade column in \code{ranking}, or
\code{NULL} (default) to auto-detect -- it accepts \code{grades_lamb0.25} and the
Stata-style \code{grades_lamb025}.}

\item{scale}{Character scalar; \code{"percent"} (default) puts the returned \code{r2}
column on a 0-100 scale, \code{"proportion"} on a 0-1 scale.}
}
\value{
A one-row data frame: \code{n} (units) and \code{grade_count}, the resolved
\code{grade_col}, the variance components \code{overall_variance}/\code{overall_sd} and
\code{between_variance}/\code{between_sd}, the \code{r2_proportion}, and \code{r2} on the
requested \code{scale} (recorded in the \code{scale} column).
}
\description{
\code{gp_krw_r2()} ports the Kline-Rose-Walters companion script
\code{code/rsquared.do}. It builds the bias-corrected denominator from the source
\verb{theta_estimates_*} columns \code{log_dif} and \code{log_dif_se}, and the between-grade
numerator from the ranking-output posterior second moments
(\code{post_mean_beta}, \code{pmean_sq}). This is the paper-faithful producer for the
firm-level KRW race/gender R-squared rows; for the quick generic
variance-share diagnostic use \code{\link[=gp_r2]{gp_r2()}}.
}
\details{
The denominator subtracts the mean sampling variance from the raw
estimate variance (the bias correction); the between-grade numerator applies
the posterior second-moment correction from the ranking columns. The names
pipeline uses different probability-scale quantities and is deferred to the
\code{M1_NAMES} milestone, so this producer is firm-level race/gender only.
}
\examples{
# The KRW race/gender inputs are CSV products of the published solve, not part
# of the tiny example fixture; build the frame columns this recipe expects.
theta <- data.frame(
  log_dif    = c(-0.4, -0.1, 0.2, 0.5),
  log_dif_se = c(0.10, 0.12, 0.09, 0.11)
)
ranking <- data.frame(
  obs_idx         = 1:4,
  post_mean_beta  = c(-0.35, -0.08, 0.18, 0.46),
  pmean_sq        = c(0.130, 0.012, 0.038, 0.220),
  grades_lamb0.25 = c(1L, 2L, 2L, 3L)
)
gp_krw_r2(theta, ranking)

}
\seealso{
\code{\link[=gp_r2]{gp_r2()}}, \code{\link[=gp_frontier]{gp_frontier()}}, \code{\link[=gp_report_card]{gp_report_card()}}

Other gradepath-frontier:
\code{\link[=gp_frontier]{gp_frontier()}},
\code{\link[=gp_r2]{gp_r2()}}
}
\concept{gradepath-frontier}
