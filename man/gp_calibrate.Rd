% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{gp_calibrate}
\alias{gp_calibrate}
\title{Seeded Monte-Carlo calibration of the one-level KRW grading method}
\usage{
gp_calibrate(
  fit,
  n_sim = 200L,
  seed = 15238L,
  dr_target = 0.05,
  ci_level = 0.9,
  min_ok = 1L
)
}
\arguments{
\item{fit}{A \code{gp_fit} from \code{\link[=krw_report_card]{krw_report_card()}} / \code{\link[=gradepath]{gradepath()}} whose fitted
prior, precision exponent \code{beta} (and additive \code{mu} for gender), and observed
standard errors \code{s_i} define the data-generating truth.}

\item{n_sim}{Integer; number of Monte-Carlo synthetic datasets to draw and
re-fit. Default \code{200L}. Use a small value (e.g. \code{5L}) for a fast check; each
draw triggers one grade-IP solve.}

\item{seed}{Integer; RNG seed for in-package reproducibility. Default \code{15238L}
(a cosmetic homage to the reference seed -- it does NOT reproduce the Matlab
stream). The same \code{seed} yields identical output; the caller's RNG stream is
restored afterward.}

\item{dr_target}{Numeric in \verb{[0, 1]}; the target discordance rate the mean
empirical DR is compared against for the \code{dr_ok} verdict (passes when within
\code{0.01}). Default \code{0.05}.}

\item{ci_level}{Numeric in the open interval \verb{(0, 1)}; the nominal
credible-interval level, also used as each refit's reporting-interval level.
The \code{coverage_ok} verdict passes when mean coverage is within \code{0.02} of this.
Default \code{0.90}.}

\item{min_ok}{Integer \verb{>= 1}; the minimum number of successful (non-skipped)
draws required before the harness reports. If fewer draws succeed it errors
rather than averaging noise. Default \code{1L}.}
}
\value{
A validated \code{gp_calibration} object (a list of class
\code{c("gp_calibration", "list")}) summarizing the run: \describe{
\item{\code{n_sim}, \code{seed}}{Integer; the requested number of draws and the RNG seed
used.}
\item{\code{characteristic}}{\code{"race"} or \code{"gender"}; the truth's demographic.}
\item{\code{n_ok}, \code{n_failed}}{Integer; successful and skipped draw counts
(\code{n_ok + n_failed == n_sim}).}
\item{\code{dr_mean}, \code{dr_target}}{Numeric in \verb{[0, 1]}; the mean empirical
discordance rate over successful draws and the target it is compared with.}
\item{\code{coverage}, \code{ci_level}}{Numeric; mean theta-scale interval coverage over
successful draws and the nominal level it is compared with.}
\item{\code{regret_mean}}{Numeric; mean regret of the empirical grading versus the
true-prior oracle (non-negative by construction).}
\item{\code{dr_ok}, \code{coverage_ok}}{Logical; the two qualitative verdicts (DR within
\code{0.01} of target; coverage within \code{0.02} of \code{ci_level}).}
\item{\code{provenance}}{Named list: producer, selected \code{lambda}, \code{beta_truth},
\code{n_units}, oracle and coverage-scale tags, and \code{matlab_rng_parity = FALSE}.}
}
}
\description{
\code{gp_calibrate()} is a standalone, seeded, qualitative Monte-Carlo calibration
harness. It treats a fitted prior as ground
truth, simulates \code{n_sim} synthetic datasets from it, re-runs the full one-level
pipeline (\code{\link[=krw_report_card]{krw_report_card()}}) on each, and checks that the grading method's own
frequentist guarantees hold qualitatively:

\itemize{
\item the empirical discordance rate (DR) tracks \code{dr_target};
\item posterior credible intervals cover the truth near \code{ci_level};
\item the empirical grading's regret against an oracle that KNOWS the true
prior is small and non-negative.
}

It is NOT a replication of KRW Table E1 (there is no parseable E1 artifact and
the Matlab Monte-Carlo stream cannot be reproduced; see the calibration scope
in the \code{m5-two-level-and-calibration} vignette), is NOT seeded to match Matlab (the \code{15238}
default is a cosmetic homage; the seed only fixes in-package reproducibility),
and is NOT a headline result or part of any default scorecard. It never runs on
package load or on vignette render. The result is a compact \code{gp_calibration}
object: scalar summaries plus the two boolean verdicts \code{dr_ok} / \code{coverage_ok}.
}
\details{
The harness is robust to refit non-convergence. Each draw's refit is wrapped in
\code{tryCatch}; a failed refit (e.g. a singular step-1 GMM moment covariance) or a
degenerate refit (an implausibly large \verb{|beta|} that corrupts the r-scale) is
skipped and counted in \code{n_failed}. The reported means are over the successful
draws only. If fewer than \code{min_ok} draws succeed, \code{gp_calibrate()} errors rather
than averaging noise.

Two scale subtleties are handled internally: the ORACLE grading is built from
the known true prior WITHOUT re-deconvolving (its pairwise comes from
\code{gp_posterior_weights(prior = truth, ...)}), and both gradings are scored on
that same truth pairwise, so regret is non-negative by construction. COVERAGE is
assessed on the beta-invariant theta scale (the refit's theta-scale reporting
interval against the true \code{theta_i = s_i^beta * v_i}), which stays coherent even
when a refit's estimated beta differs from the truth's.

Reproducibility: the whole draw loop runs under a single state-preserving seeded
context, so the same \code{seed} yields identical results and the caller's global RNG
stream is left untouched.

Cost: each draw re-solves a grade integer program once, so a run costs about
\code{n_sim} refit-and-grade solves. The function prints a one-line budget heads-up
(an ordinary, suppressible \code{message()}) before the loop whenever
\verb{n_sim * <number of units>} exceeds 600; the message is informational and never
changes the result. For a quick check use a small \code{n_sim}.

On tiny or weakly identified fits the verdicts are frequently \code{FALSE} -- the
small-N KRW beta-GMM is noisy and its r-scale can be ill-identified -- so
meaningful calibration needs a well-identified fit (KRW's real multi-firm input).
The harness reports honestly either way; it never tunes the data to force a pass.
}
\note{
Not a headline result: the published Table E1 Monte-Carlo is deferred --
gp_calibrate is a qualitative in-package diagnostic, not an E1 reproduction.
See the \code{m5-two-level-and-calibration} vignette.
}
\examples{
# A pre-solved tiny one-level fit bundled with the package.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))

# A live run re-solves the grade IP once per draw, so it needs a backend and is
# slow; keep n_sim small. (The harness also prints a budget heads-up before the
# loop when n_sim * n_units > 600.)
\donttest{
cal <- gp_calibrate(fit, n_sim = 5, seed = 15238)
cal                       # compact, result-first one-liner
summary(cal)              # one-row typed data frame
}

}
\seealso{
\code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_grade]{gp_grade()}}, \code{\link[=gp_report_card]{gp_report_card()}}
}
\concept{gradepath-calibrate}
