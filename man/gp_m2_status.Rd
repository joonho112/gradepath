% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2-status.R
\name{gp_m2_status}
\alias{gp_m2_status}
\title{Summarize the M2 acceptance status}
\usage{
gp_m2_status(acceptance = NULL)
}
\arguments{
\item{acceptance}{Optional \code{gp_m2_acceptance} object to summarize. When \code{NULL}
(default) a fresh \code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}} (the recorded-snapshot scorecard) is
built and summarized.}
}
\value{
A \code{gp_m2_status} object: a data frame of class
\code{c("gp_m2_status", "data.frame")} with one row per component
(\code{formal}, \code{L01_industry_grade_counts}, \code{L02_race_continuous},
\code{L02_gender_continuous}, \code{N10_support_guards}, \code{M1_status_boundary}) and the
columns \code{component}, \code{status}, \code{detail}, \code{evidence}, \code{producer_status},
\code{source}. It carries the attributes \code{m2_acceptance} (the backing
\code{gp_m2_acceptance}), \code{m2_formal_status} (\code{"PARTIAL_ACCEPTED"} by default), and
\code{m1_status_recalculated} (always \code{FALSE}).
}
\description{
\code{gp_m2_status()} returns a compact public summary of the current M2 acceptance
ledger -- one row per status component -- without touching the M1 status
contract in \code{R/status.R}. It is the one-screen read of \code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}} and
the verb \code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}} points users to while the M2 surface is
\code{PARTIAL_ACCEPTED}.
}
\details{
The default summary is derived from \code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}}. It reports the formal
M2 status, L01 exact industry grade-count evidence, L02 race/gender
fixture-promotion status, and N10 synthetic support-guard evidence. The default
M2 state remains \code{PARTIAL_ACCEPTED}: race L02 continuous rows remain approximate
because the recorded \code{Pi_theta} fixture gap is above \code{0.01}, while gender L02
rows are promoted to \code{banded} by fixture parity.

This helper is M2-only. It does not use \code{new_gp_status()}, does not edit
\code{R/status.R}, and does not recalculate the M1 acceptance state; the returned
object's \code{m1_status_recalculated} attribute is always \code{FALSE}.
}
\examples{
# Instant; no solve. The per-component M2 ledger summary.
gp_m2_status()

# The headline formal status is also an attribute.
attr(gp_m2_status(), "m2_formal_status")   # "PARTIAL_ACCEPTED"

}
\seealso{
\code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}}, \code{\link[=gp_m2_promoted_registry]{gp_m2_promoted_registry()}},
\code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}}

Other gradepath-twolevel:
\code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}},
\code{\link[=gp_m2_promoted_registry]{gp_m2_promoted_registry()}},
\code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}},
\code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}},
\code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}}
}
\concept{gradepath-twolevel}
