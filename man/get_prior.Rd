% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_prior}
\alias{get_prior}
\alias{get_prior.default}
\alias{get_prior.gp_fit}
\title{Extract the native prior from a gradepath fit}
\usage{
get_prior(fit, ...)

\method{get_prior}{default}(fit, ...)

\method{get_prior}{gp_fit}(fit, ...)
}
\arguments{
\item{fit}{A \code{gp_fit} object.}

\item{...}{Unused; accepted for S3 method extensibility. No arguments are
forwarded; supplying any has no effect.}
}
\value{
The \code{gp_prior} object stored on \code{fit} (returned unchanged).
}
\description{
Returns the \code{gp_prior} slot of a \code{gp_fit} unchanged. The native \code{gp_prior} is
\code{eb_prior}-shaped (it carries support, density, mean, scale, diagnostics, and
metadata) but remains a gradepath object rather than inheriting from \code{eb_prior};
it is the deconvolved prior on the r-scale that the pipeline estimates. This is
how the empirical-Bayes front half -- absorbed into gradepath's native KRW core
-- is surfaced to the user.
}
\details{
Read-only accessor adopted in \code{GP-DEC-13-A}; it never recomputes and only reads
the materialized slot.
}
\examples{
# Instant: read the native prior off the bundled tiny fit.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
get_prior(fit)         # native gp_prior (eb_prior-shaped)

}
\seealso{
\code{\link[=get_posterior]{get_posterior()}}, \code{\link[=get_report_card]{get_report_card()}}

Other gradepath-accessors:
\code{\link[=as.data.frame.gp_fit]{as.data.frame.gp_fit()}},
\code{\link[=coef.gp_fit]{coef.gp_fit()}},
\code{\link[=get_control]{get_control()}},
\code{\link[=get_grades]{get_grades()}},
\code{\link[=get_pairwise]{get_pairwise()}},
\code{\link[=get_posterior]{get_posterior()}},
\code{\link[=get_report_card]{get_report_card()}}
}
\concept{gradepath-accessors}
