% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{gp_registry}
\alias{gp_registry}
\title{gradepath replication registry (curated KRW paper-value targets)}
\format{
A data frame with 69 rows and 7 columns:
\describe{
\item{id}{character. Stable target key: the \code{paper_values.csv} key
verbatim where one exists; otherwise a synthetic key (the single such
row is \code{n9_strict4_ngrades}, the N9 strict-order grade-label guard).}
\item{paper_value}{character. Published value (stored character,
CSV-faithful; coerce with \code{as.numeric}).}
\item{unit}{character. One of \code{count}, \code{percent}, \code{proportion},
\code{correlation}, \code{sd}, \code{other}.}
\item{tolerance}{character. Absolute per-id PASS tolerance (coerce to
numeric); \code{0} for exact counts.}
\item{class}{character. \code{exact}, \code{banded}, or \code{approximate} (tolerance
provenance, per the Chapter 17 ledger).}
\item{milestone}{character. \code{M1} (current one-level firm gate),
\code{M1_NAMES} (deferred names pipeline), or \code{M2} (two-level / industry).}
\item{quantity}{character. Human-readable description of the target.}
}
}
\source{
Curated from the KRW (2024) companion \code{paper_values.csv}; tolerance
classes from the package's tolerance ledger. Names rows are
retained in the registry for paper-value traceability but assigned to the
deferred \code{M1_NAMES} milestone until the names-specific NPMLE/ranking
pipeline is bundled and tested.
}
\usage{
gp_registry
}
\description{
A curated subset of the companion \code{paper_values.csv} (the published
Kline-Rose-Walters 2024 ground-truth registry), restricted to the
quantities \code{gradepath} owns and verifies. Each row carries the published
value, its unit, an absolute per-id tolerance, a tolerance class, and the
milestone at which it is asserted. It is read by the replication harness
(\code{gp_check()} / \code{gp_run_all()}); the harness embeds no tolerances of its
own. Pairwise outranking probabilities (K05--K07) deliberately have \strong{no}
registry row -- they are verified by a golden-master on the posterior
weight matrix \code{W} instead.
}
\details{
Values are sourced verbatim from the companion \code{paper_values.csv} (the
single source of truth); the \code{class}, \code{tolerance}, and \code{milestone} columns
are attached from the package's tolerance ledger. The
NPMLE name atoms use the verbatim CSV keys \code{names_npmle_mass1/2/3}. The
registry is intentionally an M1-and-M2 core subset; names rows are retained
under \code{M1_NAMES}, and Table F5/F6 industry cells are assigned to \code{M2} until
the two-level / industry report-card pipeline is live.
}
\examples{
data(gp_registry)
str(gp_registry)
# Count the rows per milestone gate.
table(gp_registry$milestone)

}
\keyword{datasets}
