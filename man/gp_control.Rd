% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{gp_control}
\alias{gp_control}
\title{Create a gradepath run-control object}
\usage{
gp_control(
  lambda_grid = seq(0, 1, by = 0.01),
  backend = getOption("gradepath.backend", "gurobi"),
  precision_rule = c("none", "krw_gmm"),
  interval_level = 0.9,
  solver_options = NULL,
  time_limit = NULL,
  mip_gap = NULL,
  seed = NULL
)
}
\arguments{
\item{lambda_grid}{Numeric vector; the frontier penalty values the grade path
is solved at. Must be unique, strictly increasing, and lie in \verb{[0, 1]}, and
must contain the parity anchors \code{0.25} (KRW's published selection) and \code{1.00}
(the endpoint warm-start). Defaults to the reference grid \code{seq(0, 1, by = 0.01)}; passing \code{NULL} restores that reference grid, and later grade-path
execution may narrow it.}

\item{backend}{Character scalar; the planned grade-IP solver backend, one of
\code{"gurobi"}, \code{"highs"}, \code{"glpk"}, or \code{"symphony"}. Defaults to the value of
\code{getOption("gradepath.backend", "gurobi")} -- the Gurobi parity backend
(the same solver KRW used), with \code{"highs"}, \code{"glpk"}, and \code{"symphony"} as
license-free open alternatives (\code{"highs"} calls the \code{highs} package
directly; \code{"glpk"} and \code{"symphony"} route through ROI plugins). The v1
\code{"roi_highs"} / \code{"roi_glpk"} / \code{"roi_symphony"} spellings are accepted and
normalized to the short names. There is no \code{"auto"}; backend availability is
checked by the solver at call time, not here.}

\item{precision_rule}{Character scalar; the precision-handling rule. \code{"none"}
keeps the raw estimate (r) scale; \code{"krw_gmm"} selects the gradepath-native
KRW beta-GMM standardization. Defaults to \code{"none"}.}

\item{interval_level}{Numeric scalar; the credible-interval level stored on
the downstream posterior and report-card objects. A probability in the open
interval \verb{(0, 1)}. Defaults to \code{0.90}.}

\item{solver_options}{Named list; backend-specific solver options. The
portable keys \code{time_limit} and \code{mip_gap} (and their compatibility spellings
\code{max_time} and \code{mip_rel_gap}) are range-checked in place; any other key
passes through untouched. \code{mip_gap} is honored by Gurobi, HiGHS, and
SYMPHONY but not by the current GLPK route. Defaults to an empty list.}

\item{time_limit}{Numeric scalar or \code{NULL}; an ergonomic top-level alias for
the solver time limit in seconds (finite, strictly positive), merged into
\code{solver_options$time_limit}. Supplying it alongside \code{time_limit} (or its
\code{max_time} spelling) inside \code{solver_options} is an error. Defaults to \code{NULL}
(no time limit set here).}

\item{mip_gap}{Numeric scalar or \code{NULL}; an ergonomic top-level alias for the
relative MIP optimality gap in \verb{[0, 1]}, merged into
\code{solver_options$mip_gap}. Supplying it alongside \code{mip_gap} (or its
\code{mip_rel_gap} spelling) inside \code{solver_options} is an error. Defaults to
\code{NULL} (no gap target set here).}

\item{seed}{Integer scalar or \code{NULL}; a non-negative seed for the stochastic
(two-level Monte-Carlo) paths. Defaults to \code{NULL} (unseeded).}
}
\value{
A validated S3 object of class \code{c("gp_control", "list")}: a named list
carrying the six user-facing computational fields plus two internal audit
slots. \describe{
\item{\code{lambda_grid}}{Numeric vector; the validated frontier penalty grid.}
\item{\code{backend}}{Character scalar; the canonical short backend name.}
\item{\code{precision_rule}}{Character scalar; \code{"none"} or \code{"krw_gmm"}.}
\item{\code{interval_level}}{Numeric scalar in \verb{(0, 1)}; the credible-interval
level.}
\item{\code{solver_options}}{Named list; the normalized solver options, with any
ergonomic \code{time_limit} / \code{mip_gap} aliases merged in.}
\item{\code{seed}}{Non-negative integer or \code{NULL}; the stochastic-path seed.}
\item{\code{schema_version}, \code{provenance}}{Internal audit slots.}
}
}
\description{
\code{gp_control()} bundles the run settings the gradepath verbs read -- the
frontier penalty grid, the grade-IP solver backend, the precision-handling
rule, the credible-interval level, and solver runtime knobs -- into one
validated \code{gp_control} object. Every argument has a sane default, so
\code{gp_control()} with no arguments is a complete, valid configuration. Build one,
tweak the fields you care about, and pass it as the \verb{control =} argument to
\code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_grade_path]{gp_grade_path()}}, or \code{\link[=gp_preview]{gp_preview()}}.
}
\details{
The surface is the pruned v2 control of six computational fields; v1's
\code{workflow}, \code{replication_mode}, \code{target_bundle}, \code{selection_rule},
\code{backend_fallback}, and the legacy ranking vocabulary are gone. Selection of
the frontier penalty lives with the grading verbs, not here. Portable solver
runtime knobs may be passed either inside \code{solver_options} or via the
ergonomic top-level aliases \code{time_limit} and \code{mip_gap}; supplying the same
knob twice -- including a compatibility spelling alongside its ergonomic alias
-- is an error.
}
\examples{
# Runnable with no arguments and no solve: the friendly defaults give backend
# "gurobi", interval_level 0.90, and the reference lambda grid.
ctl <- gp_control()

# A customized control on an open backend: a narrow grid (still carrying the
# 0.25 and 1.00 anchors) with an ergonomic time limit merged into
# solver_options.
ctl2 <- gp_control(
  lambda_grid = c(0, 0.25, 0.5, 1),
  backend     = "highs",
  time_limit  = 30
)
ctl2$solver_options$time_limit

}
\seealso{
\code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_grade_path]{gp_grade_path()}}, \code{\link[=gp_preview]{gp_preview()}}

Other gradepath-pipeline:
\code{\link[=krw_report_card]{krw_report_card()}}
}
\concept{gradepath-pipeline}
