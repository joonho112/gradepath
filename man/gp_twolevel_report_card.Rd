% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/industry-grading.R
\name{gp_twolevel_report_card}
\alias{gp_twolevel_report_card}
\title{Return a two-level industry report card}
\usage{
gp_twolevel_report_card(twolevel, model = c("industry_rfe", "btwn"), ...)
}
\arguments{
\item{twolevel}{A \code{gp_twolevel_grade} object, or any input accepted by
\code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}} (which is then graded first).}

\item{model}{\code{"industry_rfe"} (default) for the firm-level two-level report card
or \code{"btwn"} for the between-industry report card.}

\item{...}{Arguments forwarded to \code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}} when \code{twolevel} is not
already a graded \code{gp_twolevel_grade} (e.g. \code{posterior}, \code{control}, \code{lambda}).}
}
\value{
A validated \code{gp_report_card} for the chosen route: the per-unit
grade-label table with posterior summaries.
}
\description{
\code{gp_twolevel_report_card()} is the convenience selector for the report cards
assembled by \code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}}. Passing an existing \code{gp_twolevel_grade}
never re-solves; any other input is forwarded to \code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}} first.
Choose the firm-level (\code{industry_rfe}) or between-industry (\code{btwn}) card with
\code{model}.
}
\details{
A report card exists only when the underlying two-level grade was built WITH
posterior information. This helper does not create new acceptance evidence, set
\code{PROMOTED} / \code{banded} status, or certify paper industry DR / tau / R2
reproduction; it simply returns the posterior-backed \code{gp_report_card} already
carried by the chosen route.
}
\note{
While the M2 industry surface is \code{PARTIAL_ACCEPTED}, no public
input-to-industry-card builder is exported, so there is no public way to
construct a posterior-backed two-level grade. Consequently a grade built from
bare Pi matrices (\code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}} -> \code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}}) is
structural only and carries no report card: calling this helper on it raises an
informative error directing you to \code{\link[=gp_m2_status]{gp_m2_status()}}. The success path is shown
in the package's two-level vignettes; see also \code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}}.
}
\examples{
# The M2 industry surface is PARTIAL_ACCEPTED: there is no public posterior-backed
# two-level grade to build here, so the runnable example inspects the M2 contract.
gp_m2_status()
attr(gp_m2_status(), "m2_formal_status")   # "PARTIAL_ACCEPTED"

\dontrun{
# By design this errors: a bare-Pi two-level grade is structural only and has no
# report card to return (no public builder is exported yet). See gp_m2_status().
strict <- function(n, p) {
  id <- paste0(p, seq_len(n)); m <- matrix(0, n, n, dimnames = list(id, id))
  for (i in seq_len(n)) for (j in seq_len(n)) if (i != j) m[i, j] <- if (i < j) 0.95 else 0.05
  m
}
pw <- gp_twolevel_pairwise(strict(4, "f"), strict(3, "i"),
                           control = gp_control(backend = "highs"))
gp_twolevel_report_card(pw, control = gp_control(backend = "highs"))
}

}
\seealso{
\code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}}, \code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}},
\code{\link[=gp_m2_status]{gp_m2_status()}}, \code{\link[=gp_report_card]{gp_report_card()}}

Other gradepath-twolevel:
\code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}},
\code{\link[=gp_m2_promoted_registry]{gp_m2_promoted_registry()}},
\code{\link[=gp_m2_status]{gp_m2_status()}},
\code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}},
\code{\link[=gp_twolevel_pairwise]{gp_twolevel_pairwise()}}
}
\concept{gradepath-twolevel}
