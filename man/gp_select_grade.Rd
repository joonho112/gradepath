% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grade.R
\name{gp_select_grade}
\alias{gp_select_grade}
\title{Select a stored grade fit from a solved grade path}
\usage{
gp_select_grade(path, lambda = 0.25)
}
\arguments{
\item{path}{A \link{gp_grade_path} object; the solved frontier to select from.}

\item{lambda}{Numeric scalar; the penalty to select, which must be present in
\code{path$lambda_grid}. Defaults to the KRW baseline \code{0.25}.}
}
\value{
The stored \code{gp_grade_fit} at \code{lambda} (a list of class
\code{c("gp_grade_fit", "list")}), exactly as solved on the path -- no re-solve.
See \code{\link[=gp_grade]{gp_grade()}} for the slot-level description of a \code{gp_grade_fit}.
}
\description{
\code{gp_select_grade()} returns the pre-solved \code{gp_grade_fit} at a chosen penalty
from a \link{gp_grade_path}. It never runs a solver: it indexes into the path's
stored fits, so it is the instant way to read off one assignment after
\code{\link[=gp_grade_path]{gp_grade_path()}} has done the work. If \code{lambda} is absent from the solved
grid, it errors and reports the available grid values.
}
\examples{
# Solve-free: pull the baseline-penalty fit from the bundled tiny fit's path.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
gp_select_grade(fit$grade_path, lambda = 0.25)

# The pipeline already stored this selection; it is the same object.
identical(gp_select_grade(fit$grade_path, lambda = 0.25), fit$selected_grade)

}
\seealso{
\code{\link[=gp_grade_path]{gp_grade_path()}}, \code{\link[=gp_grade]{gp_grade()}}, \code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_pairwise]{gp_pairwise()}}

Other gradepath-grade:
\code{\link[=gp_grade]{gp_grade()}},
\code{\link[=gp_grade_path]{gp_grade_path()}},
\code{\link[=gp_preview]{gp_preview()}}
}
\concept{gradepath-grade}
