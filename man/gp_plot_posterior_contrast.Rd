% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-frontier.R
\name{gp_plot_posterior_contrast}
\alias{gp_plot_posterior_contrast}
\title{Plot the grade-sorted posterior-contrast heatmap}
\usage{
gp_plot_posterior_contrast(
  x,
  threshold = TRUE,
  selected_grades = NULL,
  endpoint_grades = NULL,
  selected_lambda = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{gp_fit} (primary): uses \code{x$pairwise$matrix}, \code{x$selected_grade},
and \code{x$grade_path}. A \code{gp_pairwise} is also accepted together with explicit
\code{selected_grades} (and optionally \code{endpoint_grades}) vectors plus
\code{selected_lambda}.}

\item{threshold}{Logical; outline cells reaching the different-grade threshold
\code{1 / (1 + selected_lambda)}. Default \code{TRUE}.}

\item{selected_grades}{Integer grade vector (one per unit, in \code{ids} order),
required only when \code{x} is a bare \code{gp_pairwise}.}

\item{endpoint_grades}{Integer grade vector used as the secondary sort key;
defaults to \code{selected_grades} when omitted. Only used with a \code{gp_pairwise}.}

\item{selected_lambda}{Numeric selected lambda used for the threshold,
required only when \code{x} is a bare \code{gp_pairwise} (read from the fit otherwise).}

\item{...}{Reserved for future arguments; currently unused.}
}
\value{
A \code{ggplot} object (class \code{c("gg", "ggplot")}); it renders only when
printed.
}
\description{
\code{gp_plot_posterior_contrast()} draws the Kline-Rose-Walters grade-sorted
posterior-contrast heatmap (their Figure 5a / 12a): the J x J pairwise
outranking matrix \eqn{\Pi} (\eqn{\pi_{ij}}, the posterior probability that
unit \code{i} is more extreme than unit \code{j}), with rows and columns reordered by
\verb{(selected grade, endpoint grade, id)} so the grade blocks are contiguous.
Black segments separate the grade blocks; when \code{threshold = TRUE}, cells whose
contrast reaches the different-grade threshold \eqn{1 / (1 + \lambda)} are
outlined in red. Pass it a \code{gp_fit} (it reads the pairwise and grades from the
fit).
}
\examples{
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))

gp_plot_posterior_contrast(fit)                    # grade-sorted Pi heatmap
gp_plot_posterior_contrast(fit, threshold = FALSE) # no different-grade outline

}
\seealso{
\code{\link[=gp_plot_frontier]{gp_plot_frontier()}}, \code{\link[=theme_gradepath]{theme_gradepath()}}, \code{\link[=gp_pairwise]{gp_pairwise()}}

Other gradepath-plots:
\code{\link[=autoplot.gp_frontier]{autoplot.gp_frontier()}},
\code{\link[=autoplot.gp_report_card]{autoplot.gp_report_card()}},
\code{\link[=gp_plot_discordance]{gp_plot_discordance()}},
\code{\link[=gp_plot_frontier]{gp_plot_frontier()}},
\code{\link[=gp_plot_report_card]{gp_plot_report_card()}},
\code{\link[=scale_color_gradepath]{scale_color_gradepath()}},
\code{\link[=theme_gradepath]{theme_gradepath()}}
}
\concept{gradepath-plots}
