% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_posterior}
\alias{get_posterior}
\alias{get_posterior.default}
\alias{get_posterior.gp_fit}
\title{Extract the native posterior from a gradepath fit}
\usage{
get_posterior(fit, ...)

\method{get_posterior}{default}(fit, ...)

\method{get_posterior}{gp_fit}(fit, ...)
}
\arguments{
\item{fit}{A \code{gp_fit} object.}

\item{...}{Unused; accepted for S3 method extensibility. No arguments are
forwarded; supplying any has no effect.}
}
\value{
The \code{gp_posterior} object stored on \code{fit} (returned unchanged).
}
\description{
Returns the \code{gp_posterior} slot of a \code{gp_fit} unchanged. The native
\code{gp_posterior} is \code{eb_posterior}-shaped (per-unit posterior means, standard
deviations, and credible intervals) but remains a gradepath object rather than
inheriting from \code{eb_posterior}. It holds the per-unit posterior summaries that the
report card presents.
}
\details{
Read-only accessor adopted in \code{GP-DEC-13-A}; it never recomputes and only reads
the materialized slot.
}
\examples{
# Instant: read the native posterior off the bundled tiny fit.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
get_posterior(fit)     # native gp_posterior (eb_posterior-shaped)

}
\seealso{
\code{\link[=get_prior]{get_prior()}}, \code{\link[=get_report_card]{get_report_card()}}

Other gradepath-accessors:
\code{\link[=as.data.frame.gp_fit]{as.data.frame.gp_fit()}},
\code{\link[=coef.gp_fit]{coef.gp_fit()}},
\code{\link[=get_control]{get_control()}},
\code{\link[=get_grades]{get_grades()}},
\code{\link[=get_pairwise]{get_pairwise()}},
\code{\link[=get_prior]{get_prior()}},
\code{\link[=get_report_card]{get_report_card()}}
}
\concept{gradepath-accessors}
