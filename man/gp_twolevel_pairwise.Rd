% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/industry-grading.R
\name{gp_twolevel_pairwise}
\alias{gp_twolevel_pairwise}
\title{Wrap two-level Pi matrices as solver-ready pairwise objects}
\usage{
gp_twolevel_pairwise(
  Pi_theta,
  Pi_bar,
  ids = NULL,
  industry_levels = NULL,
  control = NULL
)
}
\arguments{
\item{Pi_theta}{Numeric square matrix; the firm-level two-level
\code{Pr(theta_i > theta_j)} outranking probabilities. Must have at least two units
and finite entries in \verb{[0, 1]}; the diagonal is forced to zero on entry.}

\item{Pi_bar}{Numeric square matrix; the between-industry \code{Pi_bar} /
\code{Pi_sbar_psi} outranking probabilities at the industry representatives. Same
shape and value constraints as \code{Pi_theta}.}

\item{ids}{Optional character vector of firm ids labelling \code{Pi_theta}. When
\code{NULL} (default), the matrix dimnames are used, or \code{unit_1}, \code{unit_2}, ... are
generated. Must be unique and match \code{nrow(Pi_theta)}.}

\item{industry_levels}{Optional character vector of industry ids labelling
\code{Pi_bar}. When \code{NULL} (default), the matrix dimnames are used, or \code{industry_1},
\code{industry_2}, ... are generated. Must be unique and match \code{nrow(Pi_bar)}.}

\item{control}{Optional \link{gp_control} object threaded onto both \code{gp_pairwise}
wrappers. When \code{NULL} (default) a default \code{gp_control()} is used.}
}
\value{
A validated \code{gp_twolevel_pairwise_bundle} object (a list of class
\code{c("gp_twolevel_pairwise_bundle", "list")}) with the public slots: \describe{
\item{\code{ids}}{Character vector; the firm ids for \code{Pi_theta}.}
\item{\code{industry_levels}}{Character vector; the industry ids for \code{Pi_bar}.}
\item{\code{raw}}{Named list \code{Pi_theta} / \code{Pi_bar}: the inputs with only the
diagonal zeroed (before the cleanup policy).}
\item{\code{Pi_theta}, \code{Pi_bar}}{The cleaned firm- and industry-level matrices
(antisymmetry, diagonal \code{0.5}, zero floor applied).}
\item{\code{pairwise_theta}, \code{pairwise_bar}}{The matching \code{gp_pairwise} objects fed
to \code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}}.}
\item{\code{cleanup}}{Named list recording the applied policy (\code{antisymmetry},
\code{diagonal}, \code{zero_floor}).}
\item{\code{control}}{The validated \link{gp_control} used.}
\item{\code{provenance}, \code{schema_version}, \code{warnings}}{Internal audit slots.}
}
}
\description{
\code{gp_twolevel_pairwise()} is the M2 pairwise dispatch for two-level industry
grading. It applies the same cleanup policy used by the one-level core
(antisymmetry, diagonal \code{0.5}, and the package zero floor) to raw two-level
outranking matrices, returning a bundle that carries both the firm-level
\code{industry_rfe} input (\code{Pi_theta}) and the between-industry \code{btwn} input
(\code{Pi_bar}) as validated \code{gp_pairwise} objects ready for \code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}}.
It is solve-free.
}
\details{
This is a structural M2 adapter, not an acceptance decision: it does not run the
fixture-promotion gate, set \code{PROMOTED} / \code{banded} status, compare industry DR,
tau, or R2 against paper targets, or update any M1 cache. M2 acceptance is the
job of \code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}} / \code{\link[=gp_m2_status]{gp_m2_status()}}.
}
\examples{
# A small synthetic strict-preference pair (the package's own test pattern):
# in strict(n, p) every off-diagonal entry is 0.95 above the diagonal and 0.05
# below, so the units are cleanly ordered. This step is solve-free.
strict <- function(n, p) {
  id <- paste0(p, seq_len(n)); m <- matrix(0, n, n, dimnames = list(id, id))
  for (i in seq_len(n)) for (j in seq_len(n)) if (i != j) m[i, j] <- if (i < j) 0.95 else 0.05
  m
}
pw <- gp_twolevel_pairwise(strict(4, "f"), strict(3, "i"),
                           control = gp_control(backend = "highs"))
pw

}
\seealso{
\code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}}, \code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}},
\code{\link[=gp_m2_status]{gp_m2_status()}}, \code{\link[=gp_control]{gp_control()}}

Other gradepath-twolevel:
\code{\link[=gp_m2_acceptance]{gp_m2_acceptance()}},
\code{\link[=gp_m2_promoted_registry]{gp_m2_promoted_registry()}},
\code{\link[=gp_m2_status]{gp_m2_status()}},
\code{\link[=gp_twolevel_grade]{gp_twolevel_grade()}},
\code{\link[=gp_twolevel_report_card]{gp_twolevel_report_card()}}
}
\concept{gradepath-twolevel}
