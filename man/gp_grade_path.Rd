% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grade.R
\name{gp_grade_path}
\alias{gp_grade_path}
\title{Solve the grade path over a lambda grid}
\usage{
gp_grade_path(
  pairwise,
  lambda_grid = NULL,
  control = NULL,
  selected_lambda = 0.25,
  selection_rule = "baseline_lambda_0.25",
  acceptance_mode = FALSE
)
}
\arguments{
\item{pairwise}{A \link{gp_pairwise} object: the \verb{J x J} posterior outranking
structure the integer program scores.}

\item{lambda_grid}{Numeric vector of penalties in \verb{[0, 1]}, or \code{NULL}. \code{NULL}
(default) uses the operational small grid unless \code{pairwise} or \code{control}
already carries a custom grid. An explicit grid must include the parity
anchors \code{0.25} and \code{1}.}

\item{control}{Optional \link{gp_control}; the backend, solver tolerances, and
active lambda grid. Defaults to \code{pairwise$control}.}

\item{selected_lambda}{Numeric scalar in the grid; the penalty recorded as the
default reportable selection on the returned path. Defaults to \code{0.25} (KRW's
published selection). Recording it never triggers a solve.}

\item{selection_rule}{Character scalar; a label stored alongside
\code{selected_lambda} in the path's selection metadata. Defaults to
\code{"baseline_lambda_0.25"}.}

\item{acceptance_mode}{Logical; the solver solution-quality policy, honored by
the gurobi backend only (open backends solve a single invocation, so it is
ignored there). \code{FALSE} (default) keeps the first Gurobi path that returns a
normalized result, so a selected solve that stops at \code{gap_reached} /
\code{time_limit} is reported honestly with that status (it lands \code{UNVERIFIED}
downstream). \code{TRUE} additionally attempts later Gurobi paths to prove the
optimum when an earlier path is not acceptance-ready; it stays honest and
never relabels a non-optimal solve as \code{optimal}. The chosen value is recorded
on each fit's \code{backend$acceptance_mode}.}
}
\value{
A validated \link{gp_grade_path} object (a list of class
\code{c("gp_grade_path", "list")}) with the public slots: \describe{
\item{\code{ids}}{Character vector; the canonical unit-id order shared by every
fit on the path.}
\item{\code{lambda_grid}}{Numeric vector; the strictly increasing penalty grid
that was solved, including the \code{0.25} and \code{1} anchors.}
\item{\code{fits}}{List of \code{gp_grade_fit}, one per \code{lambda_grid} value in
ascending order; the per-penalty grade assignments and solver status.}
\item{\code{summary}}{Data frame, one row per penalty, with \code{lambda},
\code{grade_count}, \code{discordance_rate}, \code{reliability}, \code{tau_bar}, \code{objective},
\code{raw_objective}, and \code{status} -- the frontier table.}
\item{\code{backend}}{Named list; the resolved solver name, paths, and encodings
used across the solves.}
\item{\code{selection}}{Named list (\code{selected_lambda}, \code{selection_rule},
\code{endpoint_lambda}); the recorded default selection, not a re-solve.}
\item{\code{control}}{The validated \link{gp_control} used for the run.}
\item{\code{warnings}, \code{schema_version}, \code{provenance}}{Internal audit slots
(solve order, warm-start strategy, accumulated warnings).}
}
}
\description{
\code{gp_grade_path()} solves one grade integer program per penalty value on a
lambda grid, tracing the frontier between fewer grades and lower discordance.
It is the engine the one-level pipeline calls; reach for it directly to obtain
the whole solved path, then pull a single assignment with \code{\link[=gp_select_grade]{gp_select_grade()}}
(solve-free) or solve one penalty in isolation with \code{\link[=gp_grade]{gp_grade()}}. The
Condorcet endpoint (\code{lambda = 1}) is solved first as a warm-start anchor, the
remaining penalties are solved in descending order, and fits are stored back
in ascending grid order.
}
\details{
Grade labels are contiguous integers in \verb{\{1, ..., k\}} and carry no
ranking-superiority statement of any kind. Solving descends from the
\code{lambda = 1} endpoint so each gurobi solve can warm-start from the adjacent
penalty; open backends solve each penalty independently. Selecting a fit later
(with \code{\link[=gp_select_grade]{gp_select_grade()}}) only reads the stored list and never re-solves. See
KRW (2024, Section 4) and the grade-engine method vignette for the integer
program and the frontier penalty.
}
\examples{
# Instant: read the solved path from the bundled tiny fit (24 firms).
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
fit$grade_path                 # the pre-solved gp_grade_path
fit$grade_path$summary         # the frontier table over the penalty grid

# Live solve from the stored pairwise structure; open HiGHS backend, small grid.
\donttest{
path <- gp_grade_path(
  fit$pairwise,
  control = gp_control(backend = "highs", lambda_grid = c(0.25, 1))
)
gp_select_grade(path, lambda = 0.25)
}

}
\seealso{
\code{\link[=gp_grade]{gp_grade()}}, \code{\link[=gp_select_grade]{gp_select_grade()}}, \code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_pairwise]{gp_pairwise()}}

Other gradepath-grade:
\code{\link[=gp_grade]{gp_grade()}},
\code{\link[=gp_preview]{gp_preview()}},
\code{\link[=gp_select_grade]{gp_select_grade()}}
}
\concept{gradepath-grade}
