% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_control}
\alias{get_control}
\alias{get_control.default}
\alias{get_control.gp_fit}
\title{Extract the run-control object from a gradepath fit}
\usage{
get_control(fit, ...)

\method{get_control}{default}(fit, ...)

\method{get_control}{gp_fit}(fit, ...)
}
\arguments{
\item{fit}{A \code{gp_fit} object.}

\item{...}{Unused; accepted for S3 method extensibility. No arguments are
forwarded; supplying any has no effect.}
}
\value{
The \code{gp_control} object stored on \code{fit} (returned unchanged).
}
\description{
Returns the \code{gp_control} slot of a \code{gp_fit} unchanged: the pruned run settings
actually used for the fit (\code{lambda_grid}, \code{backend}, \code{precision_rule},
\code{interval_level}, \code{solver_options}, \code{seed}). Reach for it to see exactly which
\code{\link[=gp_control]{gp_control()}} configuration produced the result, or to reuse those settings in a
follow-up run.
}
\details{
Read-only accessor adopted in \code{GP-DEC-13-A}; it never recomputes and only reads
the materialized slot.
}
\examples{
# Instant: read the run-control object off the bundled tiny fit.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
get_control(fit)       # <gp_control>

}
\seealso{
\code{\link[=gp_control]{gp_control()}}, \code{\link[=get_grades]{get_grades()}}

Other gradepath-accessors:
\code{\link[=as.data.frame.gp_fit]{as.data.frame.gp_fit()}},
\code{\link[=coef.gp_fit]{coef.gp_fit()}},
\code{\link[=get_grades]{get_grades()}},
\code{\link[=get_pairwise]{get_pairwise()}},
\code{\link[=get_posterior]{get_posterior()}},
\code{\link[=get_prior]{get_prior()}},
\code{\link[=get_report_card]{get_report_card()}}
}
\concept{gradepath-accessors}
