% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-methods.R
\name{format_gp_report_card_cli}
\alias{format_gp_report_card_cli}
\alias{summary.gp_report_card}
\alias{print.gp_report_card}
\alias{as.data.frame.gp_report_card}
\title{Format a \code{gp_report_card} for the console}
\usage{
format_gp_report_card_cli(x, ..., width = NULL, max_rows = 8L)

\method{summary}{gp_report_card}(object, ...)

\method{print}{gp_report_card}(x, ..., width = getOption("width"), max_rows = 8L)

\method{as.data.frame}{gp_report_card}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{gp_report_card}.}

\item{...}{Unused.}

\item{width}{Optional integer rendering width.}

\item{max_rows}{Integer; how many ranked rows to show before an "... N more
rows ..." line (default 8). Controls the HEAD length for large tables.}

\item{object}{A \code{gp_report_card}.}

\item{row.names, optional}{Passed through from \code{\link[base:as.data.frame]{base::as.data.frame()}};
\code{row.names} resets the payload's row names.}
}
\value{
A \code{character} vector, one element per line.

\code{summary()} returns a typed \code{gp_report_card_summary}.

\code{print()} returns \code{x} invisibly.

\code{as.data.frame()} returns the 10-column payload (\code{x$table}), the
object a researcher writes to CSV; this is the working target the print
pointer advertises.
}
\description{
Returns the plain-ASCII lines that \code{\link[=print.gp_report_card]{print.gp_report_card()}} emits, reproducing
the Chapter 12 target box. Leads with the result: unit count, grade count and
composition (e.g. \code{3 (2/81/14)}), selected lambda, a HEAD of the ranked table
(and the final row when truncated), then the frozen INVARIANT 2 welfare
footnote and pointers. Recomputes nothing.
}
\details{
The welfare note (invariant 2) describes grade 1 as the most-extreme theta and
is NOT hard-coded to one application: it flips between firms (most
discriminatory) and names (best-treated). Rows are described as "more
discriminatory" / "higher posterior contact rate", not as a contest.
}
\examples{
# The bundled fit's report card; the formatter only renders materialized rows.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
card <- get_report_card(fit)
writeLines(format_gp_report_card_cli(card))

}
\seealso{
\code{\link[=print.gp_report_card]{print.gp_report_card()}}, \code{\link[=gp_report_card]{gp_report_card()}}, \code{\link[=get_report_card]{get_report_card()}},
\code{\link[=gp_plot_report_card]{gp_plot_report_card()}}

Other gradepath-cli:
\code{\link[=format_gp_fit_cli]{format_gp_fit_cli()}},
\code{\link[=format_gp_frontier_cli]{format_gp_frontier_cli()}},
\code{\link[=format_gp_grade_fit_cli]{format_gp_grade_fit_cli()}},
\code{\link[=format_gp_grade_path_cli]{format_gp_grade_path_cli()}},
\code{\link[=format_gp_pairwise_cli]{format_gp_pairwise_cli()}}
}
\concept{gradepath-cli}
