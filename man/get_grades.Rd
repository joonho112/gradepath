% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_grades}
\alias{get_grades}
\alias{get_grades.default}
\alias{get_grades.gp_fit}
\title{Extract per-unit grades from a gradepath fit}
\usage{
get_grades(fit, ...)

\method{get_grades}{default}(fit, ...)

\method{get_grades}{gp_fit}(fit, ...)
}
\arguments{
\item{fit}{A materialized \code{gp_fit} object.}

\item{...}{Unused; accepted for S3 method extensibility. No arguments are
forwarded; supplying any has no effect.}
}
\value{
A named \code{integer} vector of length \code{J} (the number of units): the
selected grade per unit, with \code{names} equal to \code{fit$ids}.
}
\description{
Returns the selected grading as an integer vector, one entry per unit, named by
the unit ids. The grades are read from the selected \code{gp_grade_fit}
(\code{fit$selected_grade$assignment}) and aligned to the fit's canonical \code{ids} order,
so \code{get_grades(fit)[id]} is the grade of unit \code{id}. Use it as the headline
read of a \code{gp_fit}; \code{\link[=coef.gp_fit]{coef.gp_fit()}} is the base-generic synonym and
\code{\link[=get_report_card]{get_report_card()}} returns the same grades alongside the posterior summaries.
}
\details{
Per \code{GP-DEC-13-A} (Chapter 13 patterns) a \code{gp_fit} is read through the accessor
family -- \code{get_grades()}, \code{\link[=get_report_card]{get_report_card()}}, \code{\link[=get_pairwise]{get_pairwise()}}, \code{\link[=get_prior]{get_prior()}},
\code{\link[=get_posterior]{get_posterior()}}, \code{\link[=get_control]{get_control()}} -- never by reaching into \code{$} slots. The
accessor never recomputes; it only reads materialized slots. Grade labels are
integers in \verb{\{1, ..., n\}} and carry no ranking-superiority statement of any kind.
}
\examples{
# Instant: read grades off the bundled tiny fit (no solver, no Gurobi).
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
head(get_grades(fit))   # named int vector: names = ids, values = grades

}
\seealso{
\code{\link[=coef.gp_fit]{coef.gp_fit()}}, \code{\link[=get_report_card]{get_report_card()}}, \code{\link[=get_pairwise]{get_pairwise()}},
\code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_report_card]{gp_report_card()}}

Other gradepath-accessors:
\code{\link[=as.data.frame.gp_fit]{as.data.frame.gp_fit()}},
\code{\link[=coef.gp_fit]{coef.gp_fit()}},
\code{\link[=get_control]{get_control()}},
\code{\link[=get_pairwise]{get_pairwise()}},
\code{\link[=get_posterior]{get_posterior()}},
\code{\link[=get_prior]{get_prior()}},
\code{\link[=get_report_card]{get_report_card()}}
}
\concept{gradepath-accessors}
