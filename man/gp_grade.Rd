% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grade.R
\name{gp_grade}
\alias{gp_grade}
\title{Solve a single-penalty grade fit}
\usage{
gp_grade(pairwise, lambda = 0.25, control = NULL, acceptance_mode = FALSE)
}
\arguments{
\item{pairwise}{A \link{gp_pairwise} object: the \verb{J x J} posterior outranking
structure the integer program scores.}

\item{lambda}{Numeric scalar; the penalty to solve, which must be present in
the active \code{control} lambda grid. Defaults to the KRW baseline \code{0.25}.}

\item{control}{Optional \link{gp_control}; the backend, solver tolerances, and
active lambda grid. Defaults to \code{pairwise$control}.}

\item{acceptance_mode}{Logical; the solver solution-quality policy, honored by
the gurobi backend only (ignored for open backends). \code{FALSE} (default) keeps
the first Gurobi path that returns a normalized result, so a solve that stops
at \code{gap_reached} / \code{time_limit} is reported honestly with that status (it
lands \code{UNVERIFIED} downstream). \code{TRUE} additionally attempts later Gurobi
paths to prove the optimum when an earlier path is not acceptance-ready; it
stays honest and never relabels a non-optimal solve as \code{optimal}. The chosen
value is recorded on the fit's \code{backend$acceptance_mode}.}
}
\value{
A validated \code{gp_grade_fit} object (a list of class
\code{c("gp_grade_fit", "list")}) with the public slots: \describe{
\item{\code{ids}}{Character vector; the canonical unit-id order of the assignment.}
\item{\code{lambda}}{Numeric scalar; the penalty this fit was solved at.}
\item{\code{assignment}}{Data frame with one row per id: \code{id} (character) and
\code{grade} (contiguous integer label in \verb{\{1, ..., k\}}).}
\item{\code{summary}}{Named list with \code{grade_count}, \code{status}, and \code{n_units} for
the realized assignment.}
\item{\code{objective}}{Named list; the penalized and raw objective values at this
penalty.}
\item{\code{backend}}{Named list; the solver \code{name}, normalized \code{status},
\code{mipgap}, \code{runtime}, \code{acceptance_mode}, and the problem hash/signature.}
\item{\code{control}}{The validated \link{gp_control} used for the solve.}
\item{\code{warnings}, \code{schema_version}, \code{provenance}}{Internal audit slots.}
}
}
\description{
\code{gp_grade()} solves the grade integer program at exactly one penalty and
returns a \code{gp_grade_fit}. It is a targeted one-shot wrapper for diagnostics and
small workflows; for the whole frontier use \code{\link[=gp_grade_path]{gp_grade_path()}}, and to read a
penalty already on a solved path use \code{\link[=gp_select_grade]{gp_select_grade()}} (which never solves).
}
\details{
Grade labels are contiguous integers in \verb{\{1, ..., k\}} and carry no
ranking-superiority statement of any kind. Solver honesty matches
\code{\link[=gp_grade_path]{gp_grade_path()}}: a gurobi solve that stops at a gap or time limit keeps that
status, and \code{acceptance_mode = TRUE} only adds optimization attempts, never a
relabel. See KRW (2024, Section 4) and the grade-engine method vignette.
}
\examples{
# Instant: the bundled tiny fit already stores the baseline-penalty grade fit.
fit <- readRDS(system.file("extdata/examples/tiny_fit.rds", package = "gradepath"))
fit$selected_grade                 # the gp_grade_fit at lambda = 0.25
gp_select_grade(fit$grade_path, lambda = 0.25)  # the same object, no solve

# Live single-penalty solve from the stored pairwise; open HiGHS backend.
\donttest{
gp_grade(
  fit$pairwise,
  lambda = 0.25,
  control = gp_control(backend = "highs", lambda_grid = c(0.25, 1))
)
}

}
\seealso{
\code{\link[=gp_grade_path]{gp_grade_path()}}, \code{\link[=gp_select_grade]{gp_select_grade()}}, \code{\link[=krw_report_card]{krw_report_card()}}, \code{\link[=gp_pairwise]{gp_pairwise()}}

Other gradepath-grade:
\code{\link[=gp_grade_path]{gp_grade_path()}},
\code{\link[=gp_preview]{gp_preview()}},
\code{\link[=gp_select_grade]{gp_select_grade()}}
}
\concept{gradepath-grade}
